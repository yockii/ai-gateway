---
phase: 03-高级功能开发
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/user/package.json
  - frontend/user/vite.config.ts
  - frontend/user/tsconfig.json
  - frontend/user/tailwind.config.js
  - frontend/user/src/main.ts
  - frontend/user/src/App.vue
  - frontend/user/src/router/index.ts
  - frontend/user/src/api/client.ts
  - frontend/user/src/api/auth.ts
  - frontend/user/src/api/keys.ts
  - frontend/user/src/api/usage.ts
  - frontend/user/src/api/bills.ts
  - frontend/user/src/stores/auth.ts
  - frontend/user/src/stores/user.ts
  - frontend/user/src/components/layout/Sidebar.vue
  - frontend/user/src/components/layout/TopBar.vue
  - frontend/user/src/components/layout/Layout.vue
  - frontend/user/src/components/charts/UsageChart.vue
  - frontend/user/src/components/charts/CostChart.vue
  - frontend/user/src/views/Login.vue
  - frontend/user/src/views/Register.vue
  - frontend/user/src/views/Dashboard.vue
  - frontend/user/src/views/ApiKeys.vue
  - frontend/user/src/views/Usage.vue
  - frontend/user/src/views/Bills.vue
  - frontend/user/src/views/Settings.vue
autonomous: true
requirements:
  - FR-005
  - FR-007
  - FR-008
  - FR-009
  - FR-010
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
user_setup: []
must_haves:
  truths:
    - 用户可以注册和登录系统
    - 用户可以创建、查看、禁用和删除 API Key
    - 用户可以查看使用统计数据和图表
    - 用户可以查询账单并下载导出文件
    - 用户可以更新个人设置信息
    - 所有页面使用统一的经典管理后台布局
  artifacts:
    - path: frontend/user/package.json
      provides: 项目依赖配置
      min_lines: 30
    - path: frontend/user/src/api/client.ts
      provides: axios 封装和 JWT 认证
      exports: ["client", "request interceptor", "response interceptor"]
    - path: frontend/user/src/router/index.ts
      provides: 路由配置和认证守卫
      exports: ["router", "route guards"]
    - path: frontend/user/src/views/Login.vue
      provides: 登录页面
      min_lines: 80
    - path: frontend/user/src/views/ApiKeys.vue
      provides: API Key 管理页面
      min_lines: 150
    - path: frontend/user/src/components/charts/UsageChart.vue
      provides: ECharts 使用趋势图
      min_lines: 100
  key_links:
    - from: frontend/user/src/api/client.ts
      to: http://localhost:8080/v1/
      via: axios baseURL
      pattern: "baseURL.*v1"
    - from: frontend/user/src/stores/auth.ts
      to: frontend/user/src/router/index.ts
      via: Pinia store
      pattern: "useAuthStore"
    - from: frontend/user/src/views/ApiKeys.vue
      to: /v1/user/keys
      via: keysApi.list/create/delete
      pattern: "keysApi\\.(list|create|delete)"
    - from: frontend/user/src/components/charts/UsageChart.vue
      to: echarts
      via: ECharts library
      pattern: "echarts\\.init"
---

<objective>
实现用户端前端界面，基于 Vue 3 + Vite + shadcn-vue 技术栈，提供完整的用户门户功能。

Purpose: 为最终用户提供一个功能完整、界面美观的 Web 门户，用于管理 API Key、查看使用统计、查询账单和管理个人设置。

Output: 完整的用户端 Vue 3 应用，包括项目配置、API 客户端封装、路由配置、状态管理、布局组件和所有页面视图。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-高级功能开发/03-CONTEXT.md
@.planning/phases/03-高级功能开发/03-RESEARCH.md
@.planning/phases/03-高级功能开发/03-UI-SPEC.md
@.planning/ROADMAP.md

# Backend API References
@pkg/router/router.go
@internal/models/models.go
@internal/models/membership.go

# Phase 2 Context - Understanding What Was Built
@.planning/phases/02-核心功能开发/02-01-SUMMARY.md
@.planning/phases/02-核心功能开发/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 项目初始化和基础配置</name>
  <files>frontend/user/package.json, frontend/user/vite.config.ts, frontend/user/tsconfig.json, frontend/user/tailwind.config.js, frontend/user/index.html, frontend/user/src/main.ts, frontend/user/src/App.vue</files>
  <behavior>
    - 测试 1: npm run dev 成功启动开发服务器
    - 测试 2: 访问 http://localhost:5173 显示 Vue 应用
    - 测试 3: TypeScript 编译无错误
    - 测试 4: Tailwind CSS 样式正确加载
  </behavior>
  <action>
创建 Vite + Vue 3 + TypeScript 项目：

1. 使用 Vite 创建项目：
   ```bash
   cd frontend
   mkdir user
   cd user
   npm create vite@latest . -- --template vue-ts
   npm install
   ```

2. 安装核心依赖（per RESEARCH.md Standard Stack）：
   ```bash
   npm install vue-router@5 pinia axios zod echarts
   npm install -D @types/node
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p
   ```

3. 配置 Tailwind CSS（tailwind.config.js）：
   ```javascript
   export default {
     content: [
       "./index.html",
       "./src/**/*.{vue,js,ts,jsx,tsx}",
     ],
     theme: {
       extend: {},
     },
     plugins: [],
   }
   ```

4. 配置 Vite（vite.config.ts）：
   - 设置开发服务器端口 5173
   - 配置路径别名 @ 指向 ./src
   - 配置代理 /v1 到 http://localhost:8080/v1

5. 初始化 shadcn-vue：
   ```bash
   npx shadcn-vue@latest init
   ```
   - 选择 Default preset
   - 确认 components.json 配置

项目结构必须符合 CONTEXT.md D-01 规范。
  </action>
  <verify>
    <automated>cd frontend/user && npm run dev -- --port 5173 &amp;&amp; sleep 5 &amp;&amp; curl -s http://localhost:5173 | grep -q "vue"</automated>
  </verify>
  <done>Vite 开发服务器成功启动，TypeScript 编译无错误，Tailwind CSS 样式正确加载，shadcn-vue 初始化完成</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API 客户端封装和类型定义</name>
  <files>frontend/user/src/api/client.ts, frontend/user/src/types/api.ts, frontend/user/src/types/models.ts</files>
  <behavior>
    - 测试 1: client 实例正确创建，baseURL 配置正确
    - 测试 2: 请求拦截器正确添加 Authorization header
    - 测试 3: 401 响应触发清除 token 并跳转登录
    - 测试 4: TypeScript 类型定义与后端 models.go 一致
  </behavior>
  <action>
创建 axios 封装和 TypeScript 类型定义：

1. 创建 API 客户端（src/api/client.ts），per CONTEXT.md D-04 和 RESEARCH.md Pattern 1：
   - 创建 axios 实例，baseURL 从环境变量读取
   - 请求拦截器：从 localStorage 读取 access_token 并添加到 Authorization header
   - 响应拦截器：401 状态清除 token 并跳转 /login
   - 设置 timeout 为 30000ms
   - 统一错误处理，返回 Promise.reject(error)

2. 创建类型定义（src/types/models.ts），对应后端 models.go：
   ```typescript
   export interface User {
     id: string
     email: string
     name: string
     user_group_id: string
     is_active: boolean
     created_at: string
     updated_at: string
   }

   export interface UserAPIKey {
     id: string
     user_id: string
     name: string
     quota_daily: number
     quota_monthly: number
     concurrency_limit: number
     model_concurrency: Record<string, number>
     expires_at: string
     is_active: boolean
     created_at: string
     updated_at: string
   }

   export interface UsageRecord {
     id: string
     request_id: string
     user_id: string
     model_id: string
     supplier_id: string
     input_tokens: number
     output_tokens: number
     total_tokens: number
     cost_price: number
     selling_price: number
     profit: number
     created_at: string
   }

   export interface Bill {
     id: string
     user_id: string
     period: string
     start_date: string
     end_date: string
     items: BillModelDetail[]
     total_requests: number
     total_cost: number
     total_revenue: number
     total_profit: number
     status: string
     created_at: string
     updated_at: string
   }

   export interface BillModelDetail {
     model_id: string
     request_count: number
     input_tokens: number
     output_tokens: number
     total_tokens: number
     total_cost: number
     total_revenue: number
     total_profit: number
   }
   ```

3. 创建 API 响应类型（src/types/api.ts）：
   ```typescript
   export interface ApiResponse<T> {
     data: T
     message?: string
   }

   export interface ApiError {
     message: string
     code?: string
     details?: unknown
   }
   ```

确保所有类型与后端 Go 结构体字段名一致（snake_case）。
  </action>
  <verify>
    <automated>cd frontend/user && npx tsc --noEmit</automated>
  </verify>
  <done>API 客户端正确封装，JWT 认证拦截器工作正常，所有 TypeScript 类型定义与后端一致</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: API 模块和 Pinia 状态管理</name>
  <files>frontend/user/src/api/auth.ts, frontend/user/src/api/keys.ts, frontend/user/src/api/usage.ts, frontend/user/src/api/bills.ts, frontend/user/src/stores/auth.ts, frontend/user/src/stores/user.ts</files>
  <behavior>
    - 测试 1: authApi.login 发送正确格式的请求
    - 测试 2: authStore.setToken 正确保存到 localStorage
    - 测试 3: authStore.isAuthenticated 返回正确的认证状态
    - 测试 4: keysApi.list 返回 UserAPIKey 数组
  </behavior>
  <action>
创建 API 模块和 Pinia stores：

1. 创建认证 API（src/api/auth.ts）：
   ```typescript
   import client from './client'
   import type { ApiResponse, User } from '@/types/models'

   export interface LoginRequest {
     email: string
     password: string
   }

   export interface RegisterRequest {
     email: string
     password: string
     name: string
   }

   export const authApi = {
     login: (data: LoginRequest) => client.post<{ user: User; token: string }>('/user/login', data),
     register: (data: RegisterRequest) => client.post<{ user: User; token: string }>('/user/register', data),
     logout: () => client.post('/user/logout'),
     profile: () => client.get<User>('/user/profile'),
   }
   ```

2. 创建 API Key API（src/api/keys.ts）：
   ```typescript
   import client from './client'
   import type { UserAPIKey } from '@/types/models'

   export interface CreateKeyRequest {
     name: string
     quota_daily?: number
     concurrency_limit?: number
   }

   export const keysApi = {
     list: () => client.get<UserAPIKey[]>('/user/keys'),
     create: (data: CreateKeyRequest) => client.post<{ key: string; data: UserAPIKey }>('/user/keys', data),
     delete: (id: string) => client.delete(`/user/keys/${id}`),
     disable: (id: string) => client.patch(`/user/keys/${id}/disable`),
     getStats: (id: string) => client.get(`/user/keys/${id}/stats`),
   }
   ```

3. 创建使用统计 API（src/api/usage.ts）：
   ```typescript
   import client from './client'
   import type { UsageRecord } from '@/types/models'

   export const usageApi = {
     list: (params: { page?: number; limit?: number; start_date?: string; end_date?: string }) =>
       client.get<{ data: UsageRecord[]; total: number }>('/usage', { params }),
   }
   ```

4. 创建账单 API（src/api/bills.ts）：
   ```typescript
   import client from './client'
   import type { Bill } from '@/types/models'

   export const billsApi = {
     list: () => client.get<Bill[]>('/bills'),
     getById: (id: string) => client.get<Bill>(`/bills/${id}`),
     export: (id: string, format: 'pdf' | 'csv') =>
       client.post<{ download_url: string }>(`/bills/${id}/export`, { format }),
   }
   ```

5. 创建认证 Store（src/stores/auth.ts），per RESEARCH.md Pattern 4：
   ```typescript
   import { defineStore } from 'pinia'
   import { ref, computed } from 'vue'
   import type { User } from '@/types/models'
   import { authApi } from '@/api/auth'

   export const useAuthStore = defineStore('auth', () => {
     const token = ref<string | null>(localStorage.getItem('access_token'))
     const user = ref<User | null>(null)
     const loading = ref(false)

     const isAuthenticated = computed(() => !!token.value)

     const setToken = (newToken: string) => {
       token.value = newToken
       localStorage.setItem('access_token', newToken)
     }

     const setUser = (userData: User) => {
       user.value = userData
       localStorage.setItem('user_info', JSON.stringify(userData))
     }

     const login = async (email: string, password: string) => {
       loading.value = true
       try {
         const response = await authApi.login({ email, password })
         setToken(response.token)
         setUser(response.user)
       } finally {
         loading.value = false
       }
     }

     const logout = () => {
       token.value = null
       user.value = null
       localStorage.removeItem('access_token')
       localStorage.removeItem('user_info')
     }

     return { token, user, loading, isAuthenticated, setToken, setUser, login, logout }
   })
   ```

6. 创建用户 Store（src/stores/user.ts）：
   ```typescript
   import { defineStore } from 'pinia'
   import { ref } from 'vue'
   import type { User } from '@/types/models'

   export const useUserStore = defineStore('user', () => {
     const profile = ref<User | null>(null)

     const updateProfile = (data: Partial<User>) => {
       if (profile.value) {
         profile.value = { ...profile.value, ...data }
       }
     }

     return { profile, updateProfile }
   })
   ```
  </action>
  <verify>
    <automated>cd frontend/user && npx tsc --noEmit</automated>
  </verify>
  <done>所有 API 模块正确封装，Pinia stores 正确管理认证和用户状态</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 路由配置和认证守卫</name>
  <files>frontend/user/src/router/index.ts</files>
  <behavior>
    - 测试 1: 未认证用户访问受保护路由被重定向到 /login
    - 测试 2: 已认证用户访问 /login 被重定向到 /dashboard
    - 测试 3: 所有路由正确加载对应组件
  </behavior>
  <action>
创建路由配置和认证守卫，per RESEARCH.md Pattern 5：

```typescript
import { createRouter, createWebHistory } from 'vue-router'
import { useAuthStore } from '@/stores/auth'

const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    {
      path: '/login',
      name: 'Login',
      component: () => import('@/views/Login.vue'),
      meta: { requiresAuth: false }
    },
    {
      path: '/register',
      name: 'Register',
      component: () => import('@/views/Register.vue'),
      meta: { requiresAuth: false }
    },
    {
      path: '/',
      component: () => import('@/components/layout/Layout.vue'),
      meta: { requiresAuth: true },
      children: [
        {
          path: '',
          redirect: '/dashboard'
        },
        {
          path: 'dashboard',
          name: 'Dashboard',
          component: () => import('@/views/Dashboard.vue')
        },
        {
          path: 'api-keys',
          name: 'ApiKeys',
          component: () => import('@/views/ApiKeys.vue')
        },
        {
          path: 'usage',
          name: 'Usage',
          component: () => import('@/views/Usage.vue')
        },
        {
          path: 'bills',
          name: 'Bills',
          component: () => import('@/views/Bills.vue')
        },
        {
          path: 'settings',
          name: 'Settings',
          component: () => import('@/views/Settings.vue')
        }
      ]
    }
  ]
})

// 路由守卫
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()

  if (to.meta.requiresAuth && !authStore.isAuthenticated) {
    next({ name: 'Login', query: { redirect: to.fullPath } })
  } else if (to.name === 'Login' && authStore.isAuthenticated) {
    next({ name: 'Dashboard' })
  } else {
    next()
  }
})

export default router
```
  </action>
  <verify>
    <automated>cd frontend/user && npx tsc --noEmit</automated>
  </verify>
  <done>路由配置正确，认证守卫工作正常，受保护路由需要登录才能访问</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: 布局组件（Sidebar + TopBar + Layout）</name>
  <files>frontend/user/src/components/layout/Sidebar.vue, frontend/user/src/components/layout/TopBar.vue, frontend/user/src/components/layout/Layout.vue</files>
  <behavior>
    - 测试 1: 侧边栏显示所有导航项
    - 测试 2: 侧边栏可以折叠和展开
    - 测试 3: 顶部栏显示用户信息和退出按钮
    - 测试 4: 布局在移动端正确响应
  </behavior>
  <action>
创建经典管理后台布局组件，per CONTEXT.md D-05 和 UI-SPEC.md Layout Patterns：

1. 创建侧边栏组件（src/components/layout/Sidebar.vue）：
   - 宽度 240px，可折叠
   - 导航项：控制台概览、API Key 管理、使用统计、账单查询、个人设置
   - 使用 lucide-vue-next 图标
   - 折叠状态使用 Pinia store 管理
   - 高亮当前路由对应的导航项

2. 创建顶部栏组件（src/components/layout/TopBar.vue）：
   - 高度 64px
   - 左侧：汉堡菜单按钮（移动端显示）
   - 右侧：用户头像/名称、退出按钮
   - 退出按钮调用 authStore.logout()

3. 创建主布局组件（src/components/layout/Layout.vue）：
   - 组合 Sidebar 和 TopBar
   - 内容区域使用 <router-view> 渲染子路由
   - 移动端：侧边栏隐藏，点击汉堡菜单显示为 overlay
   - 桌面端：侧边栏默认展开

布局符合 UI-SPEC.md 规定的间距和响应式断点。
  </action>
  <verify>
    <automated>cd frontend/user && npx tsc --noEmit</automated>
  </verify>
  <done>布局组件正确显示，侧边栏可以折叠，移动端响应式正常</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: 认证页面（登录和注册）</name>
  <files>frontend/user/src/views/Login.vue, frontend/user/src/views/Register.vue</files>
  <behavior>
    - 测试 1: 登录表单提交正确调用 authApi.login
    - 测试 2: 登录成功后跳转到 /dashboard
    - 测试 3: 登录失败显示错误消息
    - 测试 4: 注册表单验证邮箱格式和密码强度
  </behavior>
  <action>
创建登录和注册页面，per UI-SPEC.md Form Patterns：

1. 创建登录页面（src/views/Login.vue）：
   - 使用 shadcn-vue Form 组件
   - 字段：email、password
   - zod schema 验证：email 必须为有效格式，password 至少 8 字符
   - 提交时调用 authStore.login()
   - 成功后跳转到 /dashboard 或 redirect 参数指定的页面
   - 失败时显示错误提示
   - "还没有账号？注册" 链接到 /register

2. 创建注册页面（src/views/Register.vue）：
   - 字段：name、email、password、confirmPassword
   - zod schema 验证：
     - name: 3-50 字符
     - email: 有效邮箱格式
     - password: 至少 8 字符，包含字母和数字
     - confirmPassword: 必须与 password 一致
   - 提交时调用 authApi.register()
   - 成功后自动登录并跳转到 /dashboard
   - "已有账号？登录" 链接到 /login

两个页面使用居中卡片布局，符合 UI-SPEC.md 规定的样式。
  </action>
  <verify>
    <automated>cd frontend/user && npx tsc --noEmit</automated>
  </verify>
  <done>登录和注册页面正常工作，表单验证正确，认证流程完整</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Dashboard 控制台概览页面</name>
  <files>frontend/user/src/views/Dashboard.vue</files>
  <behavior>
    - 测试 1: 页面加载时获取用户统计数据
    - 测试 2: 显示总请求数、总 Token 使用量、总费用
    - 测试 3: 图表组件正确渲染使用趋势
  </behavior>
  <action>
创建控制台概览页面，per UI-SPEC.md Copywriting Contract：

显示内容：
- 欢迎消息："欢迎回来，{用户名}"
- 3 个统计卡片：
  - API Key 数量（链接到 API Key 管理页面）
  - 本月请求数
  - 本月费用
- 使用趋势图表（使用 UsageChart 组件）
- 快速操作按钮：
  - "创建 API Key"
  - "查看账单"

使用 shadcn-vue Card 组件展示统计卡片。
空状态：暂无数据时显示"开始使用 API 服务"提示。
  </action>
  <verify>
    <automated>cd frontend/user && npx tsc --noEmit</automated>
  </verify>
  <done>Dashboard 页面正确显示用户统计数据和趋势图表</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: API Key 管理页面</name>
  <files>frontend/user/src/views/ApiKeys.vue</files>
  <behavior>
    - 测试 1: 页面加载时获取 API Key 列表
    - 测试 2: 创建 API Key 表单验证正确
    - 测试 3: 创建成功后显示完整的 key 值（仅一次）
    - 测试 4: 删除操作显示确认对话框
  </behavior>
  <action>
创建 API Key 管理页面，per UI-SPEC.md Component Inventory 和 Copywriting Contract：

功能：
- 顶部："创建 API Key" 按钮
- API Key 列表（使用 shadcn-vue Table）：
  - 列：名称、Key（脱敏显示）、每日额度、并发限制、状态、创建时间、操作
  - 操作按钮：查看详情、禁用、删除
  - 状态徽章：活跃（绿色）、禁用（灰色）

创建 API Key 对话框：
- 字段：名称、每日额度（可选）、并发限制（可选）
- 验证规则：名称 3-50 字符，额度至少为 1
- 创建成功后显示完整 key 值（仅此一次），提示用户复制
- 使用 shadcn-vue Dialog 组件

删除确认：
- 确认文本："确定要删除此 API Key 吗？此操作不可撤销。"
- 使用 shadcn-vue Dialog 或 AlertDialog

空状态："暂无 API Key" + "创建您的第一个 API Key 开始使用 AI 服务"
  </action>
  <verify>
    <automated>cd frontend/user && npx tsc --noEmit</automated>
  </verify>
  <done>API Key 管理页面正常工作，CRUD 操作完整</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9: 使用统计页面和 ECharts 图表</name>
  <files>frontend/user/src/views/Usage.vue, frontend/user/src/components/charts/UsageChart.vue, frontend/user/src/components/charts/CostChart.vue</files>
  <behavior>
    - 测试 1: UsageChart 组件正确渲染折线图
    - 测试 2: CostChart 组件正确渲染柱状图
    - 测试 3: 图表数据更新时正确重绘
    - 测试 4: 组件卸载时正确清理 ECharts 实例
  </behavior>
  <action>
创建使用统计页面和图表组件，per CONTEXT.md D-06 和 RESEARCH.md Pattern 2：

1. 创建 UsageChart 组件（src/components/charts/UsageChart.vue）：
   - 使用 ECharts 渲染折线图
   - X 轴：日期
   - Y 轴（左）：Token 使用量（柱状图）
   - Y 轴（右）：请求数（折线图）
   - 支持数据更新时重绘
   - 组件卸载时调用 dispose() 清理实例

2. 创建 CostChart 组件（src/components/charts/CostChart.vue）：
   - 使用 ECharts 渲染面积图
   - X 轴：日期
   - Y 轴：费用（元）
   - 支持按模型分组显示

3. 创建使用统计页面（src/views/Usage.vue）：
   - 顶部：日期范围选择器
   - 统计卡片：总请求数、总 Token、总费用
   - UsageChart 组件：显示使用趋势
   - CostChart 组件：显示费用趋势
   - 使用记录表格：分页显示详细记录

图表配置符合 UI-SPEC.md Data Visualization 规范。
  </action>
  <verify>
    <automated>cd frontend/user && npx tsc --noEmit</automated>
  </verify>
  <done>使用统计页面正常显示，ECharts 图表正确渲染和更新</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10: 账单查询页面</name>
  <files>frontend/user/src/views/Bills.vue</files>
  <behavior>
    - 测试 1: 页面加载时获取账单列表
    - 测试 2: 点击账单查看详情
    - 测试 3: 导出按钮调用 billsApi.export
    - 测试 4: 下载链接正确显示
  </behavior>
  <action>
创建账单查询页面，per CONTEXT.md D-08 和 UI-SPEC.md Copywriting Contract：

功能：
- 账单列表（使用 shadcn-vue Table）：
  - 列：账单期、总费用、总请求数、状态、创建时间、操作
  - 操作按钮：查看详情、导出
  - 状态徽章：待支付（黄色）、已支付（绿色）、逾期（红色）

账单详情对话框：
- 显示账单期、总费用、总请求数、总利润
- 模型明细表格（使用 BillModelDetail 数据）
- 导出按钮：PDF / CSV

导出功能：
- 点击导出调用 billsApi.export(id, format)
- 后端异步生成文件，返回下载 URL
- 显示下载链接列表（per CONTEXT.md D-08）
- 支持重新下载历史导出文件

空状态："暂无账单" + 提示信息
  </action>
  <verify>
    <automated>cd frontend/user && npx tsc --noEmit</automated>
  </verify>
  <done>账单查询页面正常工作，详情和导出功能完整</done>
</task>

<task type="auto" tdd="true">
  <name>Task 11: 个人设置页面</name>
  <files>frontend/user/src/views/Settings.vue</files>
  <behavior>
    - 测试 1: 页面加载时获取用户信息
    - 测试 2: 更新基本信息表单验证正确
    - 测试 3: 密码修改表单验证当前密码
  </behavior>
  <action>
创建个人设置页面，per UI-SPEC.md Form Patterns：

使用 shadcn-vue Tabs 组件组织设置：

Tab 1: 基本信息
- 表单字段：姓名、邮箱
- 验证规则：姓名 2-50 字符，邮箱有效格式
- 保存按钮更新用户信息

Tab 2: 安全设置
- 修改密码表单：当前密码、新密码、确认新密码
- 验证规则：新密码至少 8 字符，确认密码必须匹配
- 提交时需要验证当前密码

Tab 3: API 偏好
- 默认模型选择
- 通知偏好设置（可选）

所有表单使用 zod schema 验证。
  </action>
  <verify>
    <automated>cd frontend/user && npx tsc --noEmit</automated>
  </verify>
  <done>个人设置页面正常工作，表单验证和更新功能完整</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Untrusted user input crosses here via HTTP requests |
| localStorage → Browser | Client-side storage can be tampered with |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | API 客户端 | mitigate | JWT token 存储在 localStorage，所有请求携带 Authorization header；后端验证 token 有效性 |
| T-03-02 | Tampering | API 请求 | mitigate | 使用 HTTPS 传输，后端验证所有请求签名和参数 |
| T-03-03 | Repudiation | 用户操作 | mitigate | 关键操作（删除 API Key）记录操作日志到后端 |
| T-03-04 | Information Disclosure | API Key 显示 | mitigate | API Key 仅在创建时显示完整值，列表中脱敏显示（sk-***...） |
| T-03-05 | Denial of Service | 轮询请求 | mitigate | 运维大屏轮询间隔 30 秒，页面不可见时暂停轮询（RESEARCH.md Pitfall 4） |
| T-03-06 | Elevation of Privilege | 路由守卫 | mitigate | 前端路由守卫仅作为 UX 优化，后端必须验证所有 API 请求的权限 |
| T-03-07 | Tampering | 表单输入 | mitigate | 使用 zod schema 验证所有表单输入，防止 XSS 和注入攻击 |
| T-03-08 | Information Disclosure | 错误消息 | mitigate | 统一错误处理，不泄露敏感信息（如内部错误详情） |
</threat_model>

<verification>
1. 所有 TypeScript 文件编译无错误
2. 所有页面正确渲染，无控制台错误
3. 认证流程完整：登录 → 访问受保护页面 → 退出
4. API Key CRUD 操作正常工作
5. 图表组件正确渲染和更新
6. 路由守卫正确保护受保护页面
7. 响应式布局在移动端正常工作
8. 表单验证正确阻止无效输入
9. ECharts 实例正确清理，无内存泄漏
</verification>

<success_criteria>
1. 用户端 Vue 3 项目成功创建并运行
2. shadcn-vue 组件库正确集成
3. 所有页面（登录、注册、Dashboard、API Keys、Usage、Bills、Settings）正确实现
4. API 客户端正确封装，JWT 认证工作正常
5. Pinia 状态管理正确工作
6. 路由守卫正确保护受保护页面
7. ECharts 图表正确渲染和更新
8. 所有表单验证正确工作
9. 响应式布局在桌面和移动端正常工作
10. 代码符合项目规范，TypeScript 类型定义完整
</success_criteria>

<output>
After completion, create `.planning/phases/03-高级功能开发/03-01-SUMMARY.md`
</output>
