---
phase: 03-高级功能开发
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/admin/package.json
  - frontend/admin/vite.config.ts
  - frontend/admin/tsconfig.json
  - frontend/admin/tailwind.config.js
  - frontend/admin/src/main.ts
  - frontend/admin/src/App.vue
  - frontend/admin/src/router/index.ts
  - frontend/admin/src/api/client.ts
  - frontend/admin/src/api/users.ts
  - frontend/admin/src/api/models.ts
  - frontend/admin/src/api/suppliers.ts
  - frontend/admin/src/api/plans.ts
  - frontend/admin/src/api/monitoring.ts
  - frontend/admin/src/stores/auth.ts
  - frontend/admin/src/components/layout/Sidebar.vue
  - frontend/admin/src/components/layout/TopBar.vue
  - frontend/admin/src/components/layout/Layout.vue
  - frontend/admin/src/components/dashboard/MetricCard.vue
  - frontend/admin/src/components/dashboard/SystemStatus.vue
  - frontend/admin/src/components/tables/UserTable.vue
  - frontend/admin/src/components/tables/ModelTable.vue
  - frontend/admin/src/views/Login.vue
  - frontend/admin/src/views/Users.vue
  - frontend/admin/src/views/Models.vue
  - frontend/admin/src/views/Suppliers.vue
  - frontend/admin/src/views/Plans.vue
  - frontend/admin/src/views/Monitoring.vue
  - frontend/admin/src/views/Operations.vue
autonomous: true
requirements:
  - FR-006
  - FR-011
  - FR-012
  - FR-013
  - FR-014
  - D-01
  - D-02
  - D-09
  - D-10
  - D-11
  - D-12
user_setup: []
must_haves:
  truths:
    - 管理员可以登录系统
    - 管理员可以查看和管理用户列表
    - 管理员可以配置对外模型和路由规则
    - 管理员可以管理供应商和成本定价
    - 管理员可以配置会员套餐
    - 管理员可以在运维大屏查看实时系统数据
    - 所有管理功能使用逐行操作（非批量选择）
  artifacts:
    - path: frontend/admin/package.json
      provides: 项目依赖配置
      min_lines: 30
    - path: frontend/admin/src/api/client.ts
      provides: axios 封装和管理员认证
      exports: ["client", "admin request interceptor"]
    - path: frontend/admin/src/views/Users.vue
      provides: 用户管理页面
      min_lines: 150
    - path: frontend/admin/src/views/Operations.vue
      provides: 运维大屏页面
      min_lines: 200
    - path: frontend/admin/src/components/tables/UserTable.vue
      provides: 用户表格组件
      exports: ["UserTable", "row actions"]
  key_links:
    - from: frontend/admin/src/api/client.ts
      to: http://localhost:8080/v1/admin/
      via: axios baseURL
      pattern: "baseURL.*v1"
    - from: frontend/admin/src/views/Users.vue
      to: /v1/admin/users
      via: usersApi.list/update/delete
      pattern: "usersApi\\.(list|update|delete)"
    - from: frontend/admin/src/views/Operations.vue
      to: /v1/admin/monitoring
      via: monitoringApi.getMetrics
      pattern: "setInterval.*30000"
---

<objective>
实现运维管理端前端界面，基于 Vue 3 + Vite + shadcn-vue 技术栈，提供完整的运维管理后台和运维大屏功能。

Purpose: 为运维管理员提供一个功能完整的管理后台，用于管理用户、模型、供应商、套餐，以及监控整个系统的运行状态。

Output: 完整的运维管理端 Vue 3 应用，包括项目配置、API 客户端封装、路由配置、状态管理、布局组件、表格组件和所有管理页面。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-高级功能开发/03-CONTEXT.md
@.planning/phases/03-高级功能开发/03-RESEARCH.md
@.planning/phases/03-高级功能开发/03-UI-SPEC.md
@.planning/ROADMAP.md

# Backend API References
@pkg/router/router.go
@internal/models/models.go
@internal/models/membership.go
@internal/models/model_mapping.go

# Phase 2 Context - Understanding What Was Built
@.planning/phases/02-核心功能开发/02-03-SUMMARY.md
@.planning/phases/02-核心功能开发/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 项目初始化和基础配置</name>
  <files>frontend/admin/package.json, frontend/admin/vite.config.ts, frontend/admin/tsconfig.json, frontend/admin/tailwind.config.js, frontend/admin/index.html, frontend/admin/src/main.ts, frontend/admin/src/App.vue</files>
  <behavior>
    - 测试 1: npm run dev 成功启动开发服务器
    - 测试 2: 访问 http://localhost:5174 显示 Vue 应用
    - 测试 3: TypeScript 编译无错误
    - 测试 4: Tailwind CSS 样式正确加载
  </behavior>
  <action>
创建 Vite + Vue 3 + TypeScript 项目（运维端独立项目）：

1. 使用 Vite 创建项目：
   ```bash
   cd frontend
   mkdir admin
   cd admin
   npm create vite@latest . -- --template vue-ts
   npm install
   ```

2. 安装核心依赖（per RESEARCH.md Standard Stack）：
   ```bash
   npm install vue-router@5 pinia axios zod echarts
   npm install -D @types/node
   npm install -D tailwindcss postcss autoprefixer
   npx tailwindcss init -p
   ```

3. 配置 Tailwind CSS 和 Vite（与用户端相同配置）

4. 初始化 shadcn-vue：
   ```bash
   npx shadcn-vue@latest init
   ```

5. 配置开发服务器端口为 5174（vite.config.ts）：
   ```typescript
   server: {
     port: 5174,
     proxy: {
       '/v1': 'http://localhost:8080'
     }
   }
   ```

项目结构符合 CONTEXT.md D-01 规范，与用户端并行但独立。
  </action>
  <verify>
    <automated>cd frontend/admin && npm run dev -- --port 5174 &amp;&amp; sleep 5 &amp;&amp; curl -s http://localhost:5174 | grep -q "vue"</automated>
  </verify>
  <done>Vite 开发服务器成功启动，TypeScript 编译无错误，Tailwind CSS 样式正确加载，shadcn-vue 初始化完成</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API 客户端封装和管理员类型定义</name>
  <files>frontend/admin/src/api/client.ts, frontend/admin/src/types/api.ts, frontend/admin/src/types/models.ts</files>
  <behavior>
    - 测试 1: client 实例正确创建，baseURL 配置正确
    - 测试 2: 请求拦截器正确添加管理员认证 header
    - 测试 3: 401 响应触发清除 token 并跳转登录
    - 测试 4: TypeScript 类型定义与后端一致
  </behavior>
  <action>
创建 axios 封装和管理员 TypeScript 类型定义：

1. 创建管理员 API 客户端（src/api/client.ts）：
   - 创建 axios 实例，baseURL 指向 /v1/admin
   - 请求拦截器：从 localStorage 读取 admin_token 并添加到 Authorization header
   - 响应拦截器：401 状态清除 token 并跳转 /admin/login
   - 统一错误处理

2. 创建管理员类型定义（src/types/models.ts）：
   ```typescript
   export interface AdminUser {
     id: string
     email: string
     name: string
     role: 'superadmin' | 'admin' | 'operator'
     is_active: boolean
     created_at: string
     updated_at: string
   }

   export interface User {
     id: string
     email: string
     name: string
     user_group_id: string
     is_active: boolean
     created_at: string
   }

   export interface ExternalModel {
     id: string
     name: string
     display_name: string
     model_type: string
     is_active: boolean
     created_at: string
   }

   export interface Supplier {
     id: string
     name: string
     display_name: string
     provider: string
     is_active: boolean
     created_at: string
   }

   export interface MembershipTier {
     id: string
     name: string
     display_name: string
     level: number
     is_active: boolean
   }

   export interface SystemMetrics {
     qps: number
     avg_latency: number
     error_rate: number
     active_users: number
     total_requests: number
     cpu_usage: number
     memory_usage: number
   }
   ```

确保类型与后端 models.go 和 membership.go 一致。
  </action>
  <verify>
    <automated>cd frontend/admin && npx tsc --noEmit</automated>
  </verify>
  <done>API 客户端正确封装，管理员认证拦截器工作正常，所有 TypeScript 类型定义与后端一致</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 管理员 API 模块和认证 Store</name>
  <files>frontend/admin/src/api/auth.ts, frontend/admin/src/api/users.ts, frontend/admin/src/api/models.ts, frontend/admin/src/api/suppliers.ts, frontend/admin/src/api/plans.ts, frontend/admin/src/api/monitoring.ts, frontend/admin/src/stores/auth.ts</files>
  <behavior>
    - 测试 1: authApi.login 发送正确格式的请求
    - 测试 2: authStore.setToken 正确保存到 localStorage
    - 测试 3: usersApi.list 返回用户数组
    - 测试 4: monitoringApi.getMetrics 返回系统指标
  </behavior>
  <action>
创建管理员 API 模块和 Pinia store：

1. 创建认证 API（src/api/auth.ts）：
   ```typescript
   import client from './client'

   export interface AdminLoginRequest {
     email: string
     password: string
   }

   export const authApi = {
     login: (data: AdminLoginRequest) => client.post('/admin/login', data),
     logout: () => client.post('/admin/logout'),
     profile: () => client.get('/admin/profile'),
   }
   ```

2. 创建用户管理 API（src/api/users.ts）：
   ```typescript
   import client from './client'
   import type { User } from '@/types/models'

   export const usersApi = {
     list: (params: { page?: number; limit?: number; search?: string }) =>
       client.get<{ data: User[]; total: number }>('/admin/users', { params }),
     getById: (id: string) => client.get<User>(`/admin/users/${id}`),
     update: (id: string, data: Partial<User>) => client.put(`/admin/users/${id}`, data),
     delete: (id: string) => client.delete(`/admin/users/${id}`),
     toggleStatus: (id: string) => client.patch(`/admin/users/${id}/status`),
   }
   ```

3. 创建模型管理 API（src/api/models.ts）：
   ```typescript
   import client from './client'
   import type { ExternalModel } from '@/types/models'

   export interface CreateModelRequest {
     name: string
     display_name: string
     model_type: string
   }

   export const modelsApi = {
     list: () => client.get<ExternalModel[]>('/admin/models'),
     create: (data: CreateModelRequest) => client.post<ExternalModel>('/admin/models', data),
     update: (id: string, data: Partial<ExternalModel>) => client.put(`/admin/models/${id}`, data),
     delete: (id: string) => client.delete(`/admin/models/${id}`),
     toggleStatus: (id: string) => client.patch(`/admin/models/${id}/status`),
   }
   ```

4. 创建供应商管理 API（src/api/suppliers.ts）：
   ```typescript
   import client from './client'
   import type { Supplier } from '@/types/models'

   export const suppliersApi = {
     list: () => client.get<Supplier[]>('/admin/suppliers'),
     create: (data: Omit<Supplier, 'id' | 'created_at'>) => client.post<Supplier>('/admin/suppliers', data),
     update: (id: string, data: Partial<Supplier>) => client.put(`/admin/suppliers/${id}`, data),
     delete: (id: string) => client.delete(`/admin/suppliers/${id}`),
   }
   ```

5. 创建套餐管理 API（src/api/plans.ts）：
   ```typescript
   import client from './client'
   import type { MembershipTier } from '@/types/models'

   export const plansApi = {
     listTiers: () => client.get<MembershipTier[]>('/admin/plans/tiers'),
     createTier: (data: Omit<MembershipTier, 'id' | 'created_at'>) => client.post<MembershipTier>('/admin/plans/tiers', data),
     updateTier: (id: string, data: Partial<MembershipTier>) => client.put(`/admin/plans/tiers/${id}`, data),
     deleteTier: (id: string) => client.delete(`/admin/plans/tiers/${id}`),
   }
   ```

6. 创建监控 API（src/api/monitoring.ts）：
   ```typescript
   import client from './client'
   import type { SystemMetrics } from '@/types/models'

   export const monitoringApi = {
     getMetrics: () => client.get<SystemMetrics>('/admin/monitoring/metrics'),
     getAlerts: () => client.get('/admin/monitoring/alerts'),
     getLogs: (params: { page?: number; limit?: number }) =>
       client.get('/admin/monitoring/logs', { params }),
   }
   ```

7. 创建管理员认证 Store（src/stores/auth.ts）：
   ```typescript
   import { defineStore } from 'pinia'
   import { ref, computed } from 'vue'
   import type { AdminUser } from '@/types/models'
   import { authApi } from '@/api/auth'

   export const useAuthStore = defineStore('auth', () => {
     const token = ref<string | null>(localStorage.getItem('admin_token'))
     const admin = ref<AdminUser | null>(null)

     const isAuthenticated = computed(() => !!token.value)

     const login = async (email: string, password: string) => {
       const response = await authApi.login({ email, password })
       token.value = response.token
       admin.value = response.admin
       localStorage.setItem('admin_token', response.token)
     }

     const logout = () => {
       token.value = null
       admin.value = null
       localStorage.removeItem('admin_token')
     }

     return { token, admin, isAuthenticated, login, logout }
   })
   ```
  </action>
  <verify>
    <automated>cd frontend/admin && npx tsc --noEmit</automated>
  </verify>
  <done>所有管理员 API 模块正确封装，Pinia store 正确管理认证状态</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 路由配置和布局组件</name>
  <files>frontend/admin/src/router/index.ts, frontend/admin/src/components/layout/Sidebar.vue, frontend/admin/src/components/layout/TopBar.vue, frontend/admin/src/components/layout/Layout.vue</files>
  <behavior>
    - 测试 1: 未认证管理员访问受保护路由被重定向到 /admin/login
    - 测试 2: 侧边栏显示所有管理功能导航
    - 测试 3: 布局在移动端正确响应
  </behavior>
  <action>
创建路由配置和混合布局组件，per CONTEXT.md D-09 和 UI-SPEC.md Layout Patterns：

1. 创建路由配置（src/router/index.ts）：
   ```typescript
   import { createRouter, createWebHistory } from 'vue-router'
   import { useAuthStore } from '@/stores/auth'

   const router = createRouter({
     history: createWebHistory('/admin/'),
     routes: [
       {
         path: '/login',
         name: 'Login',
         component: () => import('@/views/Login.vue'),
         meta: { requiresAuth: false, layout: 'blank' }
       },
       {
         path: '/',
         component: () => import('@/components/layout/Layout.vue'),
         meta: { requiresAuth: true },
         children: [
           { path: '', redirect: '/users' },
           { path: 'users', name: 'Users', component: () => import('@/views/Users.vue') },
           { path: 'models', name: 'Models', component: () => import('@/views/Models.vue') },
           { path: 'suppliers', name: 'Suppliers', component: () => import('@/views/Suppliers.vue') },
           { path: 'plans', name: 'Plans', component: () => import('@/views/Plans.vue') },
           { path: 'monitoring', name: 'Monitoring', component: () => import('@/views/Monitoring.vue') }
         ]
       },
       {
         path: '/operations',
         name: 'Operations',
         component: () => import('@/views/Operations.vue'),
         meta: { requiresAuth: true, layout: 'fullscreen' }
       }
     ]
   })

   router.beforeEach((to, from, next) => {
     const authStore = useAuthStore()
     if (to.meta.requiresAuth && !authStore.isAuthenticated) {
       next({ name: 'Login', query: { redirect: to.fullPath } })
     } else {
       next()
     }
   })

   export default router
   ```

2. 创建侧边栏组件（src/components/layout/Sidebar.vue）：
   - 导航项：用户管理、模型管理、供应商管理、套餐管理、系统监控
   - "运维大屏" 链接使用全屏布局
   - 折叠功能与用户端相同

3. 创建顶部栏组件（src/components/layout/TopBar.vue）：
   - 显示管理员名称和角色
   - 退出按钮

4. 创建主布局组件（src/components/layout/Layout.vue）：
   - 与用户端相同的布局结构
   - 根据路由 meta.layout 切换布局模式

运维大屏使用全屏布局（无侧边栏），per CONTEXT.md D-09。
  </action>
  <verify>
    <automated>cd frontend/admin && npx tsc --noEmit</automated>
  </verify>
  <done>路由配置正确，布局组件支持经典管理和全屏运维大屏两种模式</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: 管理员登录页面</name>
  <files>frontend/admin/src/views/Login.vue</files>
  <behavior>
    - 测试 1: 登录表单提交正确调用 authApi.login
    - 测试 2: 登录成功后跳转到管理首页
    - 测试 3: 登录失败显示错误消息
  </behavior>
  <action>
创建管理员登录页面：

- 使用 shadcn-vue Form 组件
- 字段：email、password
- 验证规则：email 有效格式，password 至少 8 字符
- 提交时调用 authStore.login()
- 成功后跳转到 /users 或 redirect 参数指定页面
- 页面标题："AI Gateway - 运维管理"
- 居中卡片布局
  </action>
  <verify>
    <automated>cd frontend/admin && npx tsc --noEmit</automated>
  </verify>
  <done>管理员登录页面正常工作，认证流程完整</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: 用户管理页面和表格组件</name>
  <files>frontend/admin/src/views/Users.vue, frontend/admin/src/components/tables/UserTable.vue</files>
  <behavior>
    - 测试 1: 用户列表正确加载和分页
    - 测试 2: 搜索功能正确过滤用户
    - 测试 3: 逐行操作按钮正确工作
    - 测试 4: 状态切换功能正常
  </behavior>
  <action>
创建用户管理页面和表格组件，per CONTEXT.md D-10, D-11 和 UI-SPEC.md Component Inventory：

1. 创建 UserTable 组件（src/components/tables/UserTable.vue）：
   - 使用 shadcn-vue DataTable 组件
   - 列：邮箱、姓名、用户组、状态、创建时间、操作
   - 操作按钮（逐行）：编辑、禁用/启用、删除
   - 状态徽章：活跃（绿色）、禁用（灰色）
   - 支持分页
   - 支持按邮箱/姓名搜索

2. 创建用户管理页面（src/views/Users.vue）：
   - 顶部：搜索框
   - UserTable 组件
   - 编辑用户对话框：
     - 字段：姓名、邮箱、用户组、状态
     - 保存按钮调用 usersApi.update()
   - 删除确认对话框：
     - 确认文本："确定要删除此用户吗？所有关联数据将被永久删除。"
     - 使用 shadcn-vue AlertDialog

逐行操作模式，per CONTEXT.md D-11：每行独立操作按钮，不使用批量选择。
  </action>
  <verify>
    <automated>cd frontend/admin && npx tsc --noEmit</automated>
  </verify>
  <done>用户管理页面正常工作，表格支持搜索、分页和逐行操作</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: 模型管理页面和表格组件</name>
  <files>frontend/admin/src/views/Models.vue, frontend/admin/src/components/tables/ModelTable.vue</files>
  <behavior>
    - 测试 1: 模型列表正确加载
    - 测试 2: 创建模型表单验证正确
    - 测试 3: 模型类型选择器正确显示所有类型
  </behavior>
  <action>
创建模型管理页面和表格组件：

1. 创建 ModelTable 组件（src/components/tables/ModelTable.vue）：
   - 列：名称、显示名称、类型、状态、创建时间、操作
   - 操作按钮：编辑、切换状态、删除
   - 类型标签使用不同颜色区分（chat=蓝色，image=紫色等）

2. 创建模型管理页面（src/views/Models.vue）：
   - 顶部："添加模型" 按钮
   - ModelTable 组件
   - 创建/编辑模型对话框：
     - 字段：名称、显示名称、类型（下拉选择）
     - 类型选项：chat、completion、image、video、tts、stt、embedding、rerank
     - 使用 shadcn-vue Select 组件

空状态："暂无模型" + "添加模型" 按钮
  </action>
  <verify>
    <automated>cd frontend/admin && npx tsc --noEmit</automated>
  </verify>
  <done>模型管理页面正常工作，支持模型的 CRUD 操作</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: 供应商管理页面</name>
  <files>frontend/admin/src/views/Suppliers.vue</files>
  <behavior>
    - 测试 1: 供应商列表正确加载
    - 测试 2: 创建供应商表单验证正确
    - 测试 3: Provider 选择器正确显示
  </behavior>
  <action>
创建供应商管理页面：

- 顶部："添加供应商" 按钮
- 供应商列表表格：
  - 列：名称、显示名称、Provider、状态、创建时间、操作
  - 操作按钮：编辑、切换状态、删除
  - Provider 标签：openai、anthropic、azure 等
- 创建/编辑供应商对话框：
  - 字段：名称、显示名称、Provider（下拉选择）
  - Provider 选项：openai、anthropic、azure、google、baidu、alibaba 等
  - 使用 shadcn-vue Select 组件

使用与 UserTable 相同的表格结构，复用组件。
  </action>
  <verify>
    <automated>cd frontend/admin && npx tsc --noEmit</automated>
  </verify>
  <done>供应商管理页面正常工作，支持供应商的 CRUD 操作</done>
</task>

<task type="auto" tdd="true">
  <name>Task 9: 套餐管理页面</name>
  <files>frontend/admin/src/views/Plans.vue</files>
  <behavior>
    - 测试 1: 套餐列表正确加载
    - 测试 2: 创建套餐表单验证正确
    - 测试 3: 等级显示正确排序
  </behavior>
  <action>
创建套餐管理页面：

- 顶部："创建套餐" 按钮
- 套餐列表表格：
  - 列：名称、显示名称、等级、状态、创建时间、操作
  - 操作按钮：编辑、删除
  - 等级显示：数字越高等级越高
- 创建/编辑套餐对话框：
  - 字段：名称、显示名称、等级
  - 名称选项：basic、premium、vip
  - 等级输入：数字，默认 basic=1、premium=2、vip=3

使用与 UserTable 相同的表格结构。
  </action>
  <verify>
    <automated>cd frontend/admin && npx tsc --noEmit</automated>
  </verify>
  <done>套餐管理页面正常工作，支持套餐的 CRUD 操作</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10: 系统监控页面</name>
  <files>frontend/admin/src/views/Monitoring.vue</files>
  <behavior>
    - 测试 1: 系统指标正确加载和显示
    - 测试 2: 告警列表正确显示
    - 测试 3: 日志表格正确分页
  </behavior>
  <action>
创建系统监控页面，per UI-SPEC.md Data Visualization：

使用 shadcn-vue Tabs 组件组织内容：

Tab 1: 实时指标
- 4 个指标卡片：QPS、平均延迟、错误率、活跃用户
- 使用 MetricCard 组件
- 每 30 秒刷新一次（轮询模式）

Tab 2: 告警列表
- 告警表格：时间、级别、消息、状态
- 级别徽章：严重（红色）、警告（黄色）、信息（蓝色）

Tab 3: 操作日志
- 日志表格：时间、管理员、操作、详情
- 支持分页
- 支持按操作类型筛选

创建 MetricCard 组件（src/components/dashboard/MetricCard.vue）：
- props: title, value, unit, trend
- 显示标题、数值、单位、趋势箭头
- 趋势：正数上升（绿色）、负数下降（红色）

页面不可见时暂停轮询，per RESEARCH.md Pitfall 4。
  </action>
  <verify>
    <automated>cd frontend/admin && npx tsc --noEmit</automated>
  </verify>
  <done>系统监控页面正常工作，指标正确刷新，告警和日志正确显示</done>
</task>

<task type="auto" tdd="true">
  <name>Task 11: 运维大屏页面</name>
  <files>frontend/admin/src/views/Operations.vue, frontend/admin/src/components/dashboard/SystemStatus.vue</files>
  <behavior>
    - 测试 1: 大屏全屏显示，无侧边栏
    - 测试 2: 系统状态图表正确渲染
    - 测试 3: 数据每 30 秒自动更新
    - 测试 4: 返回按钮正确工作
  </behavior>
  <action>
创建运维大屏页面，per CONTEXT.md D-09, D-12 和 UI-SPEC.md Layout Patterns：

1. 创建 SystemStatus 组件（src/components/dashboard/SystemStatus.vue）：
   - ECharts 仪表盘：系统健康度
   - ECharts 面积图：请求量趋势
   - ECharts 饼图：模型使用分布
   - 使用 ECharts Gauge、Area、Pie 图表类型

2. 创建运维大屏页面（src/views/Operations.vue）：
   - 全屏布局，无侧边栏（per CONTEXT.md D-09）
   - 顶部：标题 "AI Gateway 运维大屏" + 返回按钮
   - 3x3 网格布局：
     - QPS 实时值（MetricCard）
     - 平均延迟（MetricCard）
     - 错误率（MetricCard）
     - 系统健康度（SystemStatus 仪表盘）
     - 请求量趋势（SystemStatus 面积图）
     - 模型分布（SystemStatus 饼图）
     - Top 5 用户表格
     - Top 5 模型表格
     - 最近告警列表

数据轮询更新，每 30 秒刷新一次，per CONTEXT.md D-12：
```typescript
let pollingTimer: number | null = null

const startPolling = () => {
  fetchMetrics()
  pollingTimer = window.setInterval(fetchMetrics, 30000)
}

onMounted(() => {
  startPolling()
  document.addEventListener('visibilitychange', handleVisibilityChange)
})

onUnmounted(() => {
  if (pollingTimer) clearInterval(pollingTimer)
  document.removeEventListener('visibilitychange', handleVisibilityChange)
})

const handleVisibilityChange = () => {
  if (document.hidden) {
    if (pollingTimer) clearInterval(pollingTimer)
  } else {
    startPolling()
  }
}
```

返回按钮导航到 /monitoring。
  </action>
  <verify>
    <automated>cd frontend/admin && npx tsc --noEmit</automated>
  </verify>
  <done>运维大屏页面正常工作，全屏显示正确，数据轮询更新，ECharts 图表正确渲染</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Admin API | Untrusted admin input crosses here via HTTP requests |
| localStorage → Browser | Client-side storage can be tampered with |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Spoofing | 管理员认证 | mitigate | JWT token 存储在 localStorage，所有请求携带 Authorization header；后端验证管理员权限 |
| T-03-10 | Tampering | 管理操作 | mitigate | 关键操作（删除用户、禁用模型）需要二次确认，后端记录审计日志 |
| T-03-11 | Repudiation | 管理员操作 | mitigate | 所有管理操作记录到后端审计日志，包含操作人、时间、操作内容 |
| T-03-12 | Information Disclosure | 敏感数据 | mitigate | 用户 API Key 不在管理后台显示，成本价格不在前端暴露 |
| T-03-13 | Elevation of Privilege | 权限验证 | mitigate | 前端路由守卫仅作为 UX 优化，后端必须验证管理员角色和权限 |
| T-03-14 | Denial of Service | 轮询请求 | mitigate | 运维大屏轮询间隔 30 秒，页面不可见时暂停轮询 |
| T-03-15 | Tampering | 表单输入 | mitigate | 使用 zod schema 验证所有表单输入，防止 XSS 和注入攻击 |
</threat_model>

<verification>
1. 所有 TypeScript 文件编译无错误
2. 所有页面正确渲染，无控制台错误
3. 管理员认证流程完整
4. 所有管理功能 CRUD 操作正常工作
5. 表格支持搜索、分页和逐行操作
6. 运维大屏全屏显示正确
7. 数据轮询更新正常工作
8. 页面不可见时轮询暂停
9. ECharts 图表正确渲染和更新
10. 响应式布局在移动端正常工作
</verification>

<success_criteria>
1. 运维管理端 Vue 3 项目成功创建并运行
2. shadcn-vue 组件库正确集成
3. 所有管理页面（登录、用户、模型、供应商、套餐、监控、运维大屏）正确实现
4. API 客户端正确封装，管理员认证工作正常
5. 表格组件支持搜索、分页和逐行操作
6. 运维大屏全屏显示，数据轮询更新
7. ECharts 图表正确渲染
8. 所有表单验证正确工作
9. 响应式布局在桌面和移动端正常工作
10. 代码符合项目规范，TypeScript 类型定义完整
</success_criteria>

<output>
After completion, create `.planning/phases/03-高级功能开发/03-02-SUMMARY.md`
</output>
