---
phase: 03-高级功能开发
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - internal/cache/redis.go
  - internal/middleware/cache.go
  - internal/services/cache_service.go
  - pkg/handlers/chat.go
  - pkg/handlers/images.go
  - pkg/handlers/audio.go
  - internal/database/database.go
  - go.mod
  - .github/workflows/security-scan.yml
  - scripts/security-scan.sh
  - scripts/benchmark.sh
autonomous: true
requirements:
  - D-14
  - D-16
  - NFR-001
  - NFR-002
  - NFR-003
user_setup: []
must_haves:
  truths:
    - API 网关响应时间 < 20ms（P95）
    - API 网关支持 10000+ QPS
    - Redis 缓存正确存储和检索热点数据
    - 数据库查询使用索引和优化策略
    - 安全扫描集成到 CI/CD 流程
    - 压力测试验证性能指标
  artifacts:
    - path: internal/cache/redis.go
      provides: Redis 客户端封装
      exports: ["RedisClient", "Get", "Set", "Delete"]
      min_lines: 80
    - path: internal/services/cache_service.go
      provides: 缓存服务层
      exports: ["CacheService", "GetModelList", "GetUserQuota"]
      min_lines: 120
    - path: internal/middleware/cache.go
      provides: 缓存中间件
      exports: ["CacheMiddleware"]
      min_lines: 60
    - path: .github/workflows/security-scan.yml
      provides: CI/CD 安全扫描配置
      min_lines: 50
  key_links:
    - from: internal/middleware/cache.go
      to: internal/cache/redis.go
      via: CacheService
      pattern: "cacheService\\.(Get|Set)"
    - from: pkg/handlers/chat.go
      to: internal/cache/redis.go
      via: cache middleware
      pattern: "middleware\\.Cache"
    - from: .github/workflows/security-scan.yml
      to: scripts/security-scan.sh
      via: GitHub Actions
      pattern: "security-scan\\.sh"
---

<objective>
实现后端性能优化和安全加固，包括 Redis 缓存策略、数据库查询优化、API 网关性能优化，以及集成安全扫描到 CI/CD 流程。

Purpose: 提升系统性能以满足 NFR 要求（响应时间 < 20ms，支持 10000+ QPS），并通过安全扫描确保代码质量。

Output: Redis 缓存层、缓存中间件、优化的数据库查询、性能测试脚本、CI/CD 安全扫描集成。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-高级功能开发/03-CONTEXT.md
@.planning/phases/03-高级功能开发/03-RESEARCH.md
@.planning/ROADMAP.md

# Backend References
@pkg/router/router.go
@internal/gateway/gateway.go
@internal/services/key_manager.go
@internal/services/membership.go
@internal/services/billing.go
@internal/services/model_router.go
@internal/models/models.go

# Phase 2 Context - Understanding What Was Built
@.planning/phases/02-核心功能开发/02-02-SUMMARY.md
@.planning/phases/02-核心功能开发/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redis 客户端封装和配置</name>
  <files>internal/cache/redis.go, internal/config/config.go</files>
  <behavior>
    - 测试 1: Redis 客户端成功连接
    - 测试 2: Set 操作正确存储数据
    - 测试 3: Get 操作正确检索数据
    - 测试 4: Delete 操作正确删除数据
  </behavior>
  <action>
创建 Redis 客户端封装，per CONTEXT.md D-14：

1. 创建 Redis 配置（internal/config/config.go 或更新现有配置）：
   ```go
   type RedisConfig struct {
       Addr     string `env:"REDIS_ADDR" envDefault:"localhost:6379"`
       Password string `env:"REDIS_PASSWORD"`
       DB       int    `env:"REDIS_DB" envDefault:"0"`
       PoolSize int    `env:"REDIS_POOL_SIZE" envDefault:"100"`
   }

   type Config struct {
       // ... 现有配置
       Redis RedisConfig
   }
   ```

2. 创建 Redis 客户端（internal/cache/redis.go）：
   ```go
   package cache

   import (
       "context"
       "fmt"
       "time"

       "github.com/redis/go-redis/v9"
   )

   type RedisClient struct {
       client *redis.Client
   }

   func NewRedisClient(addr, password string, db, poolSize int) *RedisClient {
       rdb := redis.NewClient(&redis.Options{
           Addr:     addr,
           Password: password,
           DB:       db,
           PoolSize: poolSize,
       })

       return &RedisClient{client: rdb}
   }

   func (r *RedisClient) Ping(ctx context.Context) error {
       return r.client.Ping(ctx).Err()
   }

   func (r *RedisClient) Get(ctx context.Context, key string) (string, error) {
       return r.client.Get(ctx, key).Result()
   }

   func (r *RedisClient) Set(ctx context.Context, key string, value interface{}, expiration time.Duration) error {
       return r.client.Set(ctx, key, value, expiration).Err()
   }

   func (r *RedisClient) Delete(ctx context.Context, keys ...string) error {
       return r.client.Del(ctx, keys...).Err()
   }

   func (r *RedisClient) Exists(ctx context.Context, keys ...string) (int64, error) {
       return r.client.Exists(ctx, keys...).Result()
   }

   func (r *RedisClient) Close() error {
       return r.client.Close()
   }
   ```

3. 在 main.go 中初始化 Redis 客户端：
   ```go
   redisClient := cache.NewRedisClient(
       config.Redis.Addr,
       config.Redis.Password,
       config.Redis.DB,
       config.Redis.PoolSize,
   )

   // 测试连接
   if err := redisClient.Ping(context.Background()); err != nil {
       log.Fatal("Failed to connect to Redis:", err)
   }
   ```

使用 go-redis/v9 作为 Redis 客户端库。
  </action>
  <verify>
    <automated>cd /d/projects/github.com/yockii/ai_gateway && go build ./internal/cache/...</automated>
  </verify>
  <done>Redis 客户端封装完成，连接配置正确，基本操作测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 缓存服务层实现</name>
  <files>internal/services/cache_service.go</files>
  <behavior>
    - 测试 1: GetModelList 缓存模型列表
    - 测试 2: 缓存过期后重新从数据库加载
    - 测试 3: InvalidateModelList 清除缓存
    - 测试 4: GetUserQuota 缓存用户配额
  </behavior>
  <action>
创建缓存服务层，实现热点数据缓存策略：

```go
package services

import (
    "context"
    "encoding/json"
    "fmt"
    "time"

    "github.com/yockii/ai-gateway/internal/cache"
    "github.com/yockii/ai-gateway/internal/database"
    "github.com/yockii/ai-gateway/internal/models"
)

type CacheService struct {
    redis    *cache.RedisClient
    db       *database.DB
}

func NewCacheService(redis *cache.RedisClient, db *database.DB) *CacheService {
    return &CacheService{redis: redis, db: db}
}

// 缓存键前缀
const (
    CacheKeyModelList      = "models:list"
    CacheKeyUserQuota      = "user:quota:%s"
    CacheKeyUserInfo       = "user:info:%s"
    CacheKeySupplierList   = "suppliers:list"
    CacheKeyMembershipTier = "membership:tier:%s"
)

// 缓存过期时间
const (
    CacheTTLShort  = 5 * time.Minute   // 短期缓存：频繁变化的数据
    CacheTTLMedium = 30 * time.Minute  // 中期缓存：模型列表等
    CacheTTLLong   = 2 * time.Hour     // 长期缓存：配置数据
)

// GetModelList 获取对外模型列表（带缓存）
func (s *CacheService) GetModelList(ctx context.Context) ([]models.ExternalModel, error) {
    // 尝试从缓存获取
    cached, err := s.redis.Get(ctx, CacheKeyModelList)
    if err == nil {
        var models []models.ExternalModel
        if err := json.Unmarshal([]byte(cached), &models); err == nil {
            return models, nil
        }
    }

    // 缓存未命中，从数据库加载
    var models []models.ExternalModel
    if err := s.db.Where("is_active = ?", true).Find(&models).Error; err != nil {
        return nil, err
    }

    // 写入缓存
    data, _ := json.Marshal(models)
    s.redis.Set(ctx, CacheKeyModelList, data, CacheTTLMedium)

    return models, nil
}

// InvalidateModelList 使模型列表缓存失效
func (s *CacheService) InvalidateModelList(ctx context.Context) error {
    return s.redis.Delete(ctx, CacheKeyModelList)
}

// GetUserQuota 获取用户配额（带缓存）
func (s *CacheService) GetUserQuota(ctx context.Context, userID string) (map[string]interface{}, error) {
    cacheKey := fmt.Sprintf(CacheKeyUserQuota, userID)

    // 尝试从缓存获取
    cached, err := s.redis.Get(ctx, cacheKey)
    if err == nil {
        var quota map[string]interface{}
        if err := json.Unmarshal([]byte(cached), &quota); err == nil {
            return quota, nil
        }
    }

    // 从数据库加载
    var key models.UserAPIKey
    if err := s.db.Where("user_id = ? AND is_active = ?", userID, true).First(&key).Error; err != nil {
        return nil, err
    }

    quota := map[string]interface{}{
        "quota_daily":       key.QuotaDaily,
        "quota_monthly":     key.QuotaMonthly,
        "concurrency_limit": key.ConcurrencyLimit,
    }

    // 写入缓存（短期缓存，配额变化较快）
    data, _ := json.Marshal(quota)
    s.redis.Set(ctx, cacheKey, data, CacheTTLShort)

    return quota, nil
}

// InvalidateUserQuota 使用户配额缓存失效
func (s *CacheService) InvalidateUserQuota(ctx context.Context, userID string) error {
    cacheKey := fmt.Sprintf(CacheKeyUserQuota, userID)
    return s.redis.Delete(ctx, cacheKey)
}

// GetUserInfo 获取用户信息（带缓存）
func (s *CacheService) GetUserInfo(ctx context.Context, userID string) (*models.User, error) {
    cacheKey := fmt.Sprintf(CacheKeyUserInfo, userID)

    cached, err := s.redis.Get(ctx, cacheKey)
    if err == nil {
        var user models.User
        if err := json.Unmarshal([]byte(cached), &user); err == nil {
            return &user, nil
        }
    }

    var user models.User
    if err := s.db.Where("id = ?", userID).First(&user).Error; err != nil {
        return nil, err
    }

    data, _ := json.Marshal(user)
    s.redis.Set(ctx, cacheKey, data, CacheTTLLong)

    return &user, nil
}

// GetSupplierList 获取供应商列表（带缓存）
func (s *CacheService) GetSupplierList(ctx context.Context) ([]models.Supplier, error) {
    cached, err := s.redis.Get(ctx, CacheKeySupplierList)
    if err == nil {
        var suppliers []models.Supplier
        if err := json.Unmarshal([]byte(cached), &suppliers); err == nil {
            return suppliers, nil
        }
    }

    var suppliers []models.Supplier
    if err := s.db.Where("is_active = ?", true).Find(&suppliers).Error; err != nil {
        return nil, err
    }

    data, _ := json.Marshal(suppliers)
    s.redis.Set(ctx, CacheKeySupplierList, data, CacheTTLLong)

    return suppliers, nil
}
```

缓存策略：
- 模型列表：30 分钟（中期缓存）
- 用户配额：5 分钟（短期缓存，频繁变化）
- 用户信息：2 小时（长期缓存）
- 供应商列表：2 小时（长期缓存）
  </action>
  <verify>
    <automated>cd /d/projects/github.com/yockii/ai_gateway && go build ./internal/services/...</automated>
  </verify>
  <done>缓存服务层实现完成，支持模型列表、用户配额、用户信息等热点数据缓存</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 缓存中间件实现</name>
  <files>internal/middleware/cache.go</files>
  <behavior>
    - 测试 1: GET /v1/models 响应被缓存
    - 测试 2: 缓存命中时直接返回，不查询数据库
    - 测试 3: POST/PUT/DELETE 请求跳过缓存
  </behavior>
  <action>
创建缓存中间件，自动缓存 GET 请求响应：

```go
package middleware

import (
    "fmt"
    "strings"
    "time"

    "github.com/gofiber/fiber/v3"
    "github.com/yockii/ai-gateway/internal/services"
)

// CacheMiddleware 创建基于请求路径的缓存中间件
func CacheMiddleware(cacheService *services.CacheService, ttl time.Duration) fiber.Handler {
    return func(c fiber.Ctx) error {
        // 只缓存 GET 请求
        if c.Method() != "GET" {
            return c.Next()
        }

        // 生成缓存键
        cacheKey := fmt.Sprintf("response:%s:%s", c.Path(), c.Context().QueryArgs().String())

        // 尝试从缓存获取
        if cached, err := cacheService.redis.Get(c.Context(), cacheKey); err == nil {
            c.Set("Content-Type", "application/json")
            c.Set("X-Cache", "HIT")
            return c.SendString(cached)
        }

        // 继续处理请求
        if err := c.Next(); err != nil {
            return err
        }

        // 只缓存成功的响应
        if c.Response().StatusCode() == 200 {
            responseBody := c.Response().Body()
            cacheService.redis.Set(c.Context(), cacheKey, responseBody, ttl)
            c.Set("X-Cache", "MISS")
        }

        return nil
    }
}

// InvalidateCacheMiddleware 缓存失效中间件
// 用于在数据更新时清除相关缓存
func InvalidateCacheMiddleware(cacheService *services.CacheService) fiber.Handler {
    return func(c fiber.Ctx) error {
        // 先执行请求
        if err := c.Next(); err != nil {
            return err
        }

        // 请求成功后，使相关缓存失效
        if c.Response().StatusCode() < 300 {
            path := c.Path()

            switch {
            case strings.Contains(path, "/admin/models"):
                cacheService.InvalidateModelList(c.Context())

            case strings.Contains(path, "/user/keys"):
                // 从请求中获取 user_id
                userID := c.Locals("user_id")
                if userID != nil {
                    cacheService.InvalidateUserQuota(c.Context(), userID.(string))
                }
            }
        }

        return nil
    }
}
```

在 router.go 中应用缓存中间件：
```go
// 应用缓存中间件到模型列表端点
v1.Get("/models",
    middleware.CacheMiddleware(cacheService, 30*time.Minute),
    handler.ListModels,
)
```
  </action>
  <verify>
    <automated>cd /d/projects/github.com/yockii/ai_gateway && go build ./internal/middleware/...</automated>
  </verify>
  <done>缓存中间件实现完成，GET 请求自动缓存，数据更新时自动失效相关缓存</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 数据库查询优化</name>
  <files>internal/database/database.go, pkg/handlers/chat.go</files>
  <behavior>
    - 测试 1: 所有查询使用索引
    - 测试 2: N+1 查询问题已解决
    - 测试 3: 查询响应时间 < 10ms
  </behavior>
  <action>
优化数据库查询，添加索引和解决 N+1 查询问题：

1. 在 database.go 中添加索引：
   ```go
   // 在 AutoMigrate 之后添加索引
   func AddIndexes(db *gorm.DB) error {
       // UsageRecord 索引
       db.Exec("CREATE INDEX IF NOT EXISTS idx_usage_records_user_created ON usage_records(user_id, created_at)")
       db.Exec("CREATE INDEX IF NOT EXISTS idx_usage_records_model_created ON usage_records(model_id, created_at)")

       // UserAPIKey 索引
       db.Exec("CREATE INDEX IF NOT EXISTS idx_user_api_keys_user_active ON user_api_keys(user_id, is_active)")

       // Bill 索引
       db.Exec("CREATE INDEX IF NOT EXISTS idx_bills_user_period ON bills(user_id, period)")

       return nil
   }
   ```

2. 优化账单查询（解决 N+1 问题）：
   ```go
   // 在 billing.go 中优化 GetBillByPeriod
   func (s *BillingService) GetBillByPeriod(ctx context.Context, userID, period string) (*models.Bill, error) {
       var bill models.Bill
       if err := s.db.Where("user_id = ? AND period = ?", userID, period).First(&bill).Error; err != nil {
           return nil, err
       }

       // 一次性查询所有明细，避免 N+1
       var items []models.BillItem
       if err := s.db.Where("bill_id = ?", bill.ID).Find(&items).Error; err != nil {
           return nil, err
       }

       // 转换为 BillModelDetail
       details := make([]models.BillModelDetail, len(items))
       for i, item := range items {
           details[i] = models.BillModelDetail{
               ModelID:       item.ModelID,
               RequestCount:  item.RequestCount,
               InputTokens:   item.InputTokens,
               OutputTokens:  item.OutputTokens,
               TotalTokens:   item.TotalTokens,
               TotalCost:     item.TotalCost,
               TotalRevenue:  item.TotalRevenue,
               TotalProfit:   item.TotalProfit,
           }
       }
       bill.Items = details

       return &bill, nil
   }
   ```

3. 添加查询性能监控：
   ```go
   func LogQuery(db *gorm.DB) {
       if db.Error == nil && db.Statement.SQL.String() != "" {
           elapsed := time.Since(db.Statement.StartTime)
           if elapsed > 100*time.Millisecond {
               log.Warnf("Slow query: %s took %v", db.Statement.SQL.String(), elapsed)
           }
       }
   }

   // 在 database.go 中注册回调
   db.Callback().Query().Before("gorm:query").Register("query_timer", logQueryStart)
   db.Callback().Query().After("gorm:query").Register("query_log", LogQuery)
   ```

4. 优化模型列表查询：
   ```go
   // 在 handlers/chat.go 中优化 ListModels
   func (h *Handler) ListModels(c fiber.Ctx) error {
       var models []models.ExternalModel
       // 只查询必要字段，使用索引
       if err := h.db.Select("id", "name", "display_name", "model_type").
           Where("is_active = ?", true).
           Find(&models).Error; err != nil {
           return c.Status(500).JSON(fiber.Map{"error": err.Error()})
       }
       return c.JSON(models)
   }
   ```
  </action>
  <verify>
    <automated>cd /d/projects/github.com/yockii/ai_gateway && go build ./...</automated>
  </verify>
  <done>数据库查询优化完成，添加了必要索引，解决了 N+1 查询问题，查询性能提升</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: API 网关性能优化</name>
  <files>internal/gateway/gateway.go, internal/middleware/concurrency.go</files>
  <behavior>
    - 测试 1: ChatCompletions 响应时间 < 20ms（不含上游）
    - 测试 2: 并发限制检查 < 1ms
    - 测试 3: 内存使用稳定，无泄漏
  </behavior>
  <action>
优化 API 网关性能，减少请求处理延迟：

1. 优化并发限制检查（使用 Redis Pipeline）：
   ```go
   // 在 concurrency.go 中使用 Pipeline 批量操作
   func CheckConcurrencyLimits(ctx context.Context, redis *cache.RedisClient, keyID, model string, limit int64) (bool, error) {
       pipe := redis.client.Pipeline()

       keyUser := fmt.Sprintf("concurrency:user:%s", keyID)
       keyModel := fmt.Sprintf("concurrency:model:%s:%s", keyID, model)

       incrUser := pipe.Incr(ctx, keyUser)
       incrModel := pipe.Incr(ctx, keyModel)
       expireUser := pipe.Expire(ctx, keyUser, time.Minute)
       expireModel := pipe.Expire(ctx, keyModel, time.Minute)

       _, err := pipe.Exec(ctx)
       if err != nil {
           return false, err
       }

       // 检查限制
       if incrUser.Val() > limit || incrModel.Val() > limit {
           // 回滚
           pipe.Decr(ctx, keyUser)
           pipe.Decr(ctx, keyModel)
           pipe.Exec(ctx)
           return false, nil
       }

       return true, nil
   }
   ```

2. 优化日志记录（异步写入）：
   ```go
   // 创建日志通道
   var logChannel = make(chan *models.UsageRecord, 1000)

   // 启动异步日志写入 goroutine
   func StartLogWriter(db *gorm.DB) {
       go func() {
           batch := make([]*models.UsageRecord, 0, 100)
           ticker := time.NewTicker(time.Second)

           for {
               select {
               case record := <-logChannel:
                   batch = append(batch, record)
                   if len(batch) >= 100 {
                       db.CreateInBatches(batch, 100)
                       batch = batch[:0]
                   }
               case <-ticker.C:
                   if len(batch) > 0 {
                       db.CreateInBatches(batch, 100)
                       batch = batch[:0]
                   }
               }
           }
       }()
   }

   // 在 handler 中使用
   func RecordUsageAsync(record *models.UsageRecord) {
       logChannel <- record
   }
   ```

3. 优化 JSON 序列化（使用对象池）：
   ```go
   var responsePool = sync.Pool{
       New: func() interface{} {
           return new(ChatCompletionResponse)
       },
   }

   func GetChatCompletionResponse() *ChatCompletionResponse {
       return responsePool.Get().(*ChatCompletionResponse)
   }

   func PutChatCompletionResponse(resp *ChatCompletionResponse) {
       responsePool.Put(resp)
   }
   ```

4. 添加连接池配置：
   ```go
   // 在 database.go 中配置连接池
   sqlDB, _ := db.DB()
   sqlDB.SetMaxIdleConns(10)
   sqlDB.SetMaxOpenConns(100)
   sqlDB.SetConnMaxLifetime(time.Hour)
   ```

5. 优化 Fiber 配置：
   ```go
   // 在 main.go 中优化 Fiber 配置
   app := fiber.New(fiber.Config{
       DisableStartupMessage: false,
       EnablePrintRoutes:     false,
       ServerHeader:          "AI-Gateway",
       AppName:               "AI Gateway v1.0",
       BodyLimit:             4 * 1024 * 1024, // 4MB
       ReadBufferSize:        4096,
       WriteBufferSize:       4096,
       // 启用压缩
       Compress: true,
   })
   ```
  </action>
  <verify>
    <automated>cd /d/projects/github.com/yockii/ai_gateway && go build ./...</automated>
  </verify>
  <done>API 网关性能优化完成，响应时间降低，并发处理能力提升</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: 安全扫描集成（GoSec + SonarQube）</name>
  <files>.github/workflows/security-scan.yml, scripts/security-scan.sh, sonar-project.properties</files>
  <behavior>
    - 测试 1: GoSec 扫描无高危漏洞
    - 测试 2: SonarQube 扫描质量门通过
    - 测试 3: CI/CD 流程正常运行
  </behavior>
  <action>
集成安全扫描到 CI/CD 流程，per CONTEXT.md D-16：

1. 创建 GitHub Actions 工作流（.github/workflows/security-scan.yml）：
   ```yaml
   name: Security Scan

   on:
     push:
       branches: [ main, develop ]
     pull_request:
       branches: [ main ]
     schedule:
       # 每天凌晨 2 点运行
       - cron: '0 2 * * *'

   jobs:
     gosec:
       name: GoSec Security Scan
       runs-on: ubuntu-latest
       steps:
         - name: Checkout code
           uses: actions/checkout@v4

         - name: Run GoSec
           uses: securego/gosec@master
           with:
             args: '-no-fail -fmt sarif -out gosec-results.sarif ./...'

         - name: Upload GoSec results
           uses: github/codeql-action/upload-sarif@v2
           with:
             sarif_file: gosec-results.sarif

     sonarqube:
       name: SonarQube Scan
       runs-on: ubuntu-latest
       steps:
         - name: Checkout code
           uses: actions/checkout@v4
           with:
             fetch-depth: 0

         - name: Set up Go
           uses: actions/setup-go@v5
           with:
             go-version: '1.26'

         - name: Run tests with coverage
           run: |
             go test -coverprofile=coverage.out -covermode=atomic ./...

         - name: SonarQube Scan
           uses: SonarSource/sonarqube-scan-action@master
           env:
             SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
             SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
   ```

2. 创建安全扫描脚本（scripts/security-scan.sh）：
   ```bash
   #!/bin/bash
   set -e

   echo "=== Running Security Scans ==="

   # GoSec 扫描
   echo "Running GoSec..."
   gosec -no-fail -fmt json -out gosec-report.json ./...
   gosec -severity medium -confidence medium ./...

   # GoVulnCheck 检查已知漏洞
   echo "Running GoVulnCheck..."
   govulncheck ./...

   # Golangci-lint
   echo "Running golangci-lint..."
   golangci-lint run --timeout 5m ./...

   # npm audit（如果有前端代码）
   if [ -d "frontend/user" ]; then
       echo "Running npm audit (user portal)..."
       cd frontend/user && npm audit --audit-level=high
   fi

   if [ -d "frontend/admin" ]; then
       echo "Running npm audit (admin portal)..."
       cd frontend/admin && npm audit --audit-level=high
   fi

   echo "=== Security Scans Complete ==="
   ```

3. 创建 SonarQube 配置（sonar-project.properties）：
   ```properties
   sonar.projectKey=ai-gateway
   sonar.organization=your-org
   sonar.sources=.
   sonar.exclusions=**/*_test.go,**/vendor/**,**/bin/**,**/frontend/**
   sonar.tests=.
   sonar.test.inclusions=**/*_test.go
   sonar.coverage.exclusions=**/*_test.go,**/vendor/**
   sonar.go.coverage.reportPaths=coverage.out
   sonar.qualitygate.wait=true
   ```

4. 更新 go.mod 添加安全扫描工具：
   ```bash
   go install github.com/securecodewarrior/gosec/v2/cmd/gosec@latest
   go install golang.org/x/vuln/cmd/govulncheck@latest
   go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
   ```
  </action>
  <verify>
    <automated>cd /d/projects/github.com/yockii/ai_gateway && bash scripts/security-scan.sh 2>&amp;1 | head -50</automated>
  </verify>
  <done>安全扫描集成完成，CI/CD 流程包含 GoSec 和 SonarQube 扫描</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: 性能测试脚本</name>
  <files>scripts/benchmark.sh, benchmarks/api_benchmark_test.go</files>
  <behavior>
    - 测试 1: 基准测试成功运行
    - 测试 2: API 响应时间 < 20ms
    - 测试 3: 并发测试支持 10000+ QPS
  </behavior>
  <action>
创建性能测试脚本和基准测试：

1. 创建 API 基准测试（benchmarks/api_benchmark_test.go）：
   ```go
   package benchmarks

   import (
       "encoding/json"
       "net/http"
       "testing"
       "time"

       "github.com/yockii/ai-gateway/pkg/api"
   )

   func BenchmarkChatCompletions(b *testing.B) {
       client := &http.Client{Timeout: 30 * time.Second}
       url := "http://localhost:8080/v1/chat/completions"

       req := api.ChatRequest{
           Model: "gpt-3.5-turbo",
           Messages: []api.Message{
               {Role: "user", Content: "Hello"},
           },
       }

       b.ResetTimer()
       b.ReportAllocs()

       for i := 0; i < b.N; i++ {
           body, _ := json.Marshal(req)
           httpReq, _ := http.NewRequest("POST", url, bytes.NewReader(body))
           httpReq.Header.Set("Content-Type", "application/json")
           httpReq.Header.Set("Authorization", "Bearer test-key")

           resp, err := client.Do(httpReq)
           if err != nil {
               b.Fatalf("Request failed: %v", err)
           }
           resp.Body.Close()
       }
   }

   func BenchmarkListModels(b *testing.B) {
       client := &http.Client{Timeout: 10 * time.Second}
       url := "http://localhost:8080/v1/models"

       b.ResetTimer()
       b.ReportAllocs()

       for i := 0; i < b.N; i++ {
           req, _ := http.NewRequest("GET", url, nil)
           req.Header.Set("Authorization", "Bearer test-key")

           resp, err := client.Do(req)
           if err != nil {
               b.Fatalf("Request failed: %v", err)
           }
           resp.Body.Close()
       }
   }
   ```

2. 创建性能测试脚本（scripts/benchmark.sh）：
   ```bash
   #!/bin/bash
   set -e

   echo "=== AI Gateway Performance Benchmark ==="

   # 启动服务器（后台）
   go run cmd/main.go &
   SERVER_PID=$!
   sleep 5

   # 清理函数
   cleanup() {
       kill $SERVER_PID 2>/dev/null || true
   }
   trap cleanup EXIT

   # 运行基准测试
   echo "Running Go benchmarks..."
   go test -bench=. -benchmem -benchtime=10s ./benchmarks/ | tee benchmark-results.txt

   # 使用 wrk 进行 HTTP 压力测试
   echo "Running wrk stress test..."

   # 测试 /v1/models 端点
   wrk -t4 -c100 -d30s --latency \
       -H "Authorization: Bearer test-key" \
       http://localhost:8080/v1/models

   # 测试 /v1/chat/completions 端点
   wrk -t4 -c100 -d30s --latency -s benchmarks/chat_post.lua \
       -H "Authorization: Bearer test-key" \
       http://localhost:8080/v1/chat/completions

   echo "=== Benchmark Complete ==="
   echo "Results saved to benchmark-results.txt"
   ```

3. 创建 wrk 脚本（benchmarks/chat_post.lua）：
   ```lua
   wrk.method = "POST"
   wrk.body   = '{"model":"gpt-3.5-turbo","messages":[{"role":"user","content":"Hi"}]}'
   wrk.headers["Content-Type"] = "application/json"
   ```

4. 更新 go.mod 添加测试工具：
   ```bash
   go install github.com/wg/wrk@latest
   ```
  </action>
  <verify>
    <automated>cd /d/projects/github.com/yockii/ai_gateway && go test -bench=BenchmarkListModels -benchtime=1s ./benchmarks/ 2>&amp;1 | tail -5</automated>
  </verify>
  <done>性能测试脚本创建完成，可以验证 API 网关性能指标</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Gateway → Redis | Cached data crosses here |
| API Gateway → Database | Query results cross here |
| CI/CD → Codebase | Security scanners analyze code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Spoofing | Redis 连接 | mitigate | Redis 使用密码认证，部署在内网或使用 TLS |
| T-03-17 | Tampering | 缓存数据 | mitigate | 缓存数据设置过期时间，敏感数据不缓存 |
| T-03-18 | Information Disclosure | 日志记录 | mitigate | 异步日志不包含敏感信息（API Key、密码） |
| T-03-19 | Denial of Service | 缓存穿透 | mitigate | 使用空值缓存防止穿透，限制 Redis 连接数 |
| T-03-20 | Tampering | 数据库查询 | mitigate | 使用参数化查询，防止 SQL 注入 |
| T-03-21 | Elevation | SQL 注入 | mitigate | GORM 自动参数化，禁止使用原生 SQL 拼接 |
| T-03-22 | Tampering | 依赖漏洞 | mitigate | 定期运行 govulncheck 和 npm audit，及时更新依赖 |
</threat_model>

<verification>
1. Redis 客户端成功连接，基本操作测试通过
2. 缓存服务层正确缓存和失效数据
3. 缓存中间件正确工作，X-Cache header 显示 HIT/MISS
4. 数据库查询使用索引，无 N+1 问题
5. API 网关响应时间 < 20ms（不含上游调用）
6. 并发限制检查 < 1ms
7. GoSec 扫描无高危漏洞
8. SonarQube 质量门通过
9. 性能测试脚本正常运行
10. wrk 压力测试达到 10000+ QPS
</verification>

<success_criteria>
1. Redis 缓存层正确集成，热点数据缓存生效
2. 缓存中间件自动缓存 GET 请求
3. 数据库查询优化完成，添加必要索引
4. N+1 查询问题解决
5. API 网关性能提升，响应时间 < 20ms
6. 并发处理能力达到 10000+ QPS
7. 安全扫描集成到 CI/CD 流程
8. GoSec 和 SonarQube 扫描无高危问题
9. 性能测试脚本可以验证性能指标
10. 内存使用稳定，无内存泄漏
</success_criteria>

<output>
After completion, create `.planning/phases/03-高级功能开发/03-03-SUMMARY.md`
</output>
