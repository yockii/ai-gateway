---
phase: 03-高级功能开发
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - internal/metrics/prometheus.go
  - internal/metrics/metrics.go
  - pkg/router/router.go
  - deployments/monitoring/prometheus.yml
  - deployments/monitoring/grafana/dashboards/api-gateway.json
  - deployments/monitoring/grafana/dashboards/system-metrics.json
  - deployments/monitoring/grafana/provisioning/datasources/prometheus.yml
  - deployments/monitoring/loki.yml
  - deployments/monitoring/promtail.yml
  - deployments/docker-compose.monitoring.yml
  - internal/logging/logger.go
  - internal/alerting/alerts.go
autonomous: true
requirements:
  - D-13
  - D-15
  - NFR-004
  - NFR-005
  - NFR-006
user_setup:
  - service: grafana
    why: "监控数据可视化"
    dashboard_config:
      - task: "导入 API Gateway 仪表盘"
        location: "Grafana UI → Dashboards → Import"
      - task: "配置 Prometheus 数据源"
        location: "Grafana UI → Configuration → Data Sources"
  - service: prometheus
    why: "指标收集和存储"
    env_vars:
      - name: PROMETHEUS_RETENTION
        source: "环境变量或 prometheus.yml 配置"
must_haves:
  truths:
    - Prometheus 成功采集 Go 应用指标
    - Grafana 仪表盘正确显示系统指标
    - Loki 收集应用日志并支持查询
    - 告警规则配置正确，触发时发送通知
    - 监控系统持久化数据
  artifacts:
    - path: internal/metrics/prometheus.go
      provides: Prometheus metrics 暴露
      exports: ["PrometheusMiddleware", "RecordRequest"]
      min_lines: 100
    - path: deployments/monitoring/prometheus.yml
      provides: Prometheus 配置
      min_lines: 50
    - path: deployments/monitoring/grafana/dashboards/api-gateway.json
      provides: API Gateway 仪表盘配置
      min_lines: 200
    - path: internal/logging/logger.go
      provides: 结构化日志
      exports: ["NewLogger", "WithFields"]
      min_lines: 80
  key_links:
    - from: pkg/router/router.go
      to: internal/metrics/prometheus.go
      via: PrometheusMiddleware
      pattern: "metrics\\.PrometheusMiddleware"
    - from: deployments/monitoring/prometheus.yml
      to: internal/metrics/prometheus.go
      via: /metrics endpoint
      pattern: "metrics_path: '/metrics'"
    - from: deployments/monitoring/grafana/provisioning/datasources/prometheus.yml
      to: deployments/monitoring/prometheus.yml
      via: Prometheus service
      pattern: "url: 'http://prometheus:9090'"
    - from: deployments/monitoring/promtail.yml
      to: internal/logging/logger.go
      via: Loki API
      pattern: "url: 'http://loki:3100'"
---

<objective>
实现完整的监控和日志系统，包括 Prometheus 指标暴露、Grafana 仪表盘、Loki 日志聚合和告警系统。

Purpose: 提供全面的系统可观测性，帮助运维团队监控系统健康状态、快速定位问题和响应异常。

Output: Prometheus metrics 端点、Grafana 仪表盘配置、Loki 日志聚合配置、告警规则、Docker Compose 部署配置。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-高级功能开发/03-CONTEXT.md
@.planning/phases/03-高级功能开发/03-RESEARCH.md
@.planning/ROADMAP.md

# Backend References
@pkg/router/router.go
@internal/gateway/gateway.go
@internal/middleware/middleware.go

# Phase 3 Context
@.planning/phases/03-高级功能开发/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prometheus Metrics 暴露</name>
  <files>internal/metrics/prometheus.go, internal/metrics/metrics.go, pkg/router/router.go</files>
  <behavior>
    - 测试 1: /metrics 端点返回 Prometheus 格式指标
    - 测试 2: HTTP 请求计数器正确递增
    - 测试 3: 响应时间直方图正确记录
    - 测试 4: Go 运行时指标正确暴露
  </behavior>
  <action>
创建 Prometheus metrics 暴露层，per CONTEXT.md D-13：

1. 添加 Prometheus 依赖到 go.mod：
   ```bash
   go get github.com/prometheus/client_golang/prometheus
   go get github.com/prometheus/client_golang/prometheus/promhttp
   ```

2. 创建 metrics 定义（internal/metrics/metrics.go）：
   ```go
   package metrics

   import (
       "github.com/prometheus/client_golang/prometheus"
       "github.com/prometheus/client_golang/prometheus/promauto"
   )

   var (
       // HTTP 请求总数
       HTTPRequestsTotal = promauto.NewCounterVec(
           prometheus.CounterOpts{
               Name: "http_requests_total",
               Help: "Total number of HTTP requests",
           },
           []string{"method", "endpoint", "status"},
       )

       // HTTP 请求持续时间
       HTTPRequestDuration = promauto.NewHistogramVec(
           prometheus.HistogramOpts{
               Name:    "http_request_duration_seconds",
               Help:    "HTTP request duration in seconds",
               Buckets: prometheus.DefBuckets,
           },
           []string{"method", "endpoint"},
       )

       // API 请求计数（按模型）
       APIRequestsTotal = promauto.NewCounterVec(
           prometheus.CounterOpts{
               Name: "api_requests_total",
               Help: "Total number of API requests",
           },
           []string{"model", "provider"},
       )

       // API Token 使用量
       APITokensTotal = promauto.NewCounterVec(
           prometheus.CounterOpts{
               Name: "api_tokens_total",
               Help: "Total number of API tokens consumed",
           },
           []string{"model", "type"}, // type: input/output
       )

       // API 费用
       APICostTotal = promauto.NewCounterVec(
           prometheus.CounterOpts{
               Name: "api_cost_total",
               Help: "Total API cost in currency",
           },
           []string{"model", "currency"},
       )

       // 并发连接数
       ConcurrentConnections = promauto.NewGauge(
           prometheus.GaugeOpts{
               Name: "concurrent_connections",
               Help: "Current number of concurrent connections",
           },
       )

       // 活跃用户数
       ActiveUsers = promauto.NewGauge(
           prometheus.GaugeOpts{
               Name: "active_users",
               Help: "Current number of active users",
           },
       )

       // 错误率
       ErrorsTotal = promauto.NewCounterVec(
           prometheus.CounterOpts{
               Name: "errors_total",
               Help: "Total number of errors",
           },
           []string{"type", "location"},
       )
   )
   ```

3. 创建 Prometheus 中间件（internal/metrics/prometheus.go）：
   ```go
   package metrics

   import (
       "strconv"
       "time"

       "github.com/gofiber/fiber/v3"
       "github.com/yockii/ai-gateway/internal/metrics"
   )

   // PrometheusMiddleware 记录 HTTP 请求指标
   func PrometheusMiddleware() fiber.Handler {
       return func(c fiber.Ctx) error {
           start := time.Now()

           // 增加并发连接数
           metrics.ConcurrentConnections.Inc()
           defer metrics.ConcurrentConnections.Dec()

           // 处理请求
           err := c.Next()

           // 记录指标
           duration := time.Since(start).Seconds()
           status := strconv.Itoa(c.Response().StatusCode())

           metrics.HTTPRequestsTotal.WithLabelValues(
               c.Method(),
               c.Path(),
               status,
           ).Inc()

           metrics.HTTPRequestDuration.WithLabelValues(
               c.Method(),
               c.Path(),
           ).Observe(duration)

           // 记录错误
           if err != nil || c.Response().StatusCode() >= 400 {
               metrics.ErrorsTotal.WithLabelValues(
                   "http",
                   c.Path(),
               ).Inc()
           }

           return err
       }
   }

   // RecordAPIRequest 记录 API 请求
   func RecordAPIRequest(model, provider string) {
       metrics.APIRequestsTotal.WithLabelValues(model, provider).Inc()
   }

   // RecordTokenUsage 记录 Token 使用量
   func RecordTokenUsage(model string, inputTokens, outputTokens int32) {
       metrics.APITokensTotal.WithLabelValues(model, "input").Add(float64(inputTokens))
       metrics.APITokensTotal.WithLabelValues(model, "output").Add(float64(outputTokens))
   }

   // RecordCost 记录费用
   func RecordCost(model, currency string, cost float64) {
       metrics.APICostTotal.WithLabelValues(model, currency).Add(cost)
   }
   ```

4. 在 router.go 中注册 metrics 端点：
   ```go
   import (
       "github.com/prometheus/client_golang/prometheus/promhttp"
       "github.com/yockii/ai-gateway/internal/metrics"
   )

   func Setup(app *fiber.App, gw *gateway.Gateway) {
       // 应用 Prometheus 中间件
       app.Use(metrics.PrometheusMiddleware())

       // ... 现有中间件和路由

       // Metrics 端点（不需要认证）
       app.Get("/metrics", func(c fiber.Ctx) error {
           handler := promhttp.Handler()
           handler.ServeHTTP(c.Response().BodyWriter(), c.Request())
           return nil
       })
   }
   ```

5. 添加 Go 运行时指标（在 main.go 中）：
   ```go
   import (
       "github.com/prometheus/client_golang/prometheus/promauto"
       "runtime"
   )

   // 收集 Go 运行时指标
   func collectRuntimeMetrics() {
       promauto.NewGaugeFunc(prometheus.GaugeOpts{
           Name: "go_goroutines",
           Help: "Number of goroutines",
       }, func() float64 {
           return float64(runtime.NumGoroutine())
       })

       var m runtime.MemStats
       promauto.NewGaugeFunc(prometheus.GaugeOpts{
           Name: "go_memstats_alloc_bytes",
           Help: "Current memory allocation",
       }, func() float64 {
           runtime.ReadMemStats(&m)
           return float64(m.Alloc)
       })
   }
   ```
  </action>
  <verify>
    <automated>cd /d/projects/github.com/yockii/ai_gateway && go build ./... && curl -s http://localhost:8080/metrics | grep -c "http_requests_total"</automated>
  </verify>
  <done>Prometheus metrics 端点成功暴露，所有指标正确记录和展示</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prometheus 配置和部署</name>
  <files>deployments/monitoring/prometheus.yml, deployments/docker-compose.monitoring.yml</files>
  <behavior>
    - 测试 1: Prometheus 容器成功启动
    - 测试 2: Prometheus 成功抓取 /metrics 端点
    - 测试 3: Prometheus UI 可以查询指标
  </behavior>
  <action>
创建 Prometheus 配置和 Docker Compose 部署：

1. 创建 Prometheus 配置（deployments/monitoring/prometheus.yml）：
   ```yaml
   global:
     scrape_interval: 15s
     evaluation_interval: 15s
     external_labels:
       cluster: 'ai-gateway'
       environment: 'development'

   # 告警规则文件
   rule_files:
     - 'alerts.yml'

   # 抓取配置
   scrape_configs:
     # API Gateway
     - job_name: 'ai-gateway'
       static_configs:
         - targets: ['host.docker.internal:8080']
           labels:
             service: 'api-gateway'
       metrics_path: '/metrics'

     # Prometheus 自身
     - job_name: 'prometheus'
       static_configs:
         - targets: ['localhost:9090']

   # 告警管理器（可选）
   alerting:
     alertmanagers:
       - static_configs:
           - targets: ['alertmanager:9093']
   ```

2. 创建告警规则（deployments/monitoring/alerts.yml）：
   ```yaml
   groups:
     - name: api_gateway_alerts
       interval: 30s
       rules:
         # 高错误率告警
         - alert: HighErrorRate
           expr: rate(http_requests_total{status=~"5.."}[5m]) / rate(http_requests_total[5m]) > 0.05
           for: 5m
           labels:
             severity: warning
           annotations:
             summary: "High error rate detected"
             description: "Error rate is {{ $value | humanizePercentage }} for {{ $labels.endpoint }}"

         # 高延迟告警
         - alert: HighLatency
           expr: histogram_quantile(0.95, http_request_duration_seconds_bucket) > 1
           for: 5m
           labels:
             severity: warning
           annotations:
             summary: "High latency detected"
             description: "P95 latency is {{ $value }}s for {{ $labels.endpoint }}"

         # 服务不可用告警
         - alert: ServiceDown
           expr: up{job="ai-gateway"} == 0
           for: 1m
           labels:
             severity: critical
           annotations:
             summary: "Service is down"
             description: "{{ $labels.job }} service is not responding"

         # 内存使用过高告警
         - alert: HighMemoryUsage
           expr: go_memstats_alloc_bytes / 1024 / 1024 > 512
           for: 5m
           labels:
             severity: warning
           annotations:
             summary: "High memory usage"
             description: "Memory usage is {{ $value }}MB"

         # Goroutine 泄漏告警
         - alert: HighGoroutineCount
           expr: go_goroutines > 1000
           for: 5m
           labels:
             severity: warning
           annotations:
             summary: "High goroutine count"
             description: "Goroutine count is {{ $value }}"
   ```

3. 创建 Docker Compose 配置（deployments/docker-compose.monitoring.yml）：
   ```yaml
   version: '3.8'

   services:
     # Prometheus
     prometheus:
       image: prom/prometheus:latest
       container_name: ai-gateway-prometheus
       ports:
         - "9090:9090"
       volumes:
         - ./deployments/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
         - ./deployments/monitoring/alerts.yml:/etc/prometheus/alerts.yml
         - prometheus-data:/prometheus
       command:
         - '--config.file=/etc/prometheus/prometheus.yml'
         - '--storage.tsdb.path=/prometheus'
         - '--web.console.libraries=/usr/share/prometheus/console_libraries'
         - '--web.console.templates=/usr/share/prometheus/consoles'
       extra_hosts:
         - "host.docker.internal:host-gateway"
       networks:
         - monitoring

     # Grafana
     grafana:
       image: grafana/grafana:latest
       container_name: ai-gateway-grafana
       ports:
         - "3000:3000"
       environment:
         - GF_SECURITY_ADMIN_USER=admin
         - GF_SECURITY_ADMIN_PASSWORD=admin
         - GF_USERS_ALLOW_SIGN_UP=false
       volumes:
         - ./deployments/monitoring/grafana/provisioning:/etc/grafana/provisioning
         - ./deployments/monitoring/grafana/dashboards:/var/lib/grafana/dashboards
         - grafana-data:/var/lib/grafana
       networks:
         - monitoring
       depends_on:
         - prometheus

     # Loki
     loki:
       image: grafana/loki:latest
       container_name: ai-gateway-loki
       ports:
         - "3100:3100"
       volumes:
         - ./deployments/monitoring/loki.yml:/etc/loki/local-config.yaml
         - loki-data:/tmp/loki
       command: -config.file=/etc/loki/local-config.yaml
       networks:
         - monitoring

     # Promtail
     promtail:
       image: grafana/promtail:latest
       container_name: ai-gateway-promtail
       volumes:
         - ./deployments/monitoring/promtail.yml:/etc/promtail/config.yml
         - ./logs:/var/log/ai-gateway
       command: -config.file=/etc/promtail/config.yml
       networks:
         - monitoring
       depends_on:
         - loki

   volumes:
     prometheus-data:
     grafana-data:
     loki-data:

   networks:
     monitoring:
       driver: bridge
   ```

4. 启动监控系统：
   ```bash
   docker-compose -f deployments/docker-compose.monitoring.yml up -d
   ```
  </action>
  <verify>
    <automated>docker ps --filter "name=prometheus" --format "{{.Status}}"</automated>
  </verify>
  <done>Prometheus 配置完成，容器成功启动，开始抓取 API Gateway 指标</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Grafana 仪表盘配置</name>
  <files>deployments/monitoring/grafana/provisioning/datasources/prometheus.yml, deployments/monitoring/grafana/provisioning/datasources/loki.yml, deployments/monitoring/grafana/dashboards/api-gateway.json, deployments/monitoring/grafana/dashboards/system-metrics.json</files>
  <behavior>
    - 测试 1: Grafana 成功连接 Prometheus
    - 测试 2: 仪表盘正确显示 API 指标
    - 测试 3: 仪表盘正确显示系统指标
  </behavior>
  <action>
创建 Grafana 仪表盘配置：

1. 创建 Prometheus 数据源配置（deployments/monitoring/grafana/provisioning/datasources/prometheus.yml）：
   ```yaml
   apiVersion: 1

   datasources:
     - name: Prometheus
       type: prometheus
       access: proxy
       url: http://prometheus:9090
       isDefault: true
       editable: true
   ```

2. 创建 Loki 数据源配置（deployments/monitoring/grafana/provisioning/datasources/loki.yml）：
   ```yaml
   apiVersion: 1

   datasources:
     - name: Loki
       type: loki
       access: proxy
       url: http://loki:3100
       editable: true
   ```

3. 创建仪表盘提供者配置（deployments/monitoring/grafana/provisioning/dashboards/dashboards.yml）：
   ```yaml
   apiVersion: 1

   providers:
     - name: 'API Gateway'
       orgId: 1
       folder: ''
       type: file
       disableDeletion: false
       updateIntervalSeconds: 10
       allowUiUpdates: true
       options:
         path: /var/lib/grafana/dashboards
   ```

4. 创建 API Gateway 仪表盘（deployments/monitoring/grafana/dashboards/api-gateway.json）：
   ```json
   {
     "annotations": {
       "list": []
     },
     "editable": true,
     "gnetId": null,
     "graphTooltip": 0,
     "id": null,
     "links": [],
     "panels": [
       {
         "datasource": "Prometheus",
         "fieldConfig": {
           "defaults": {
             "color": {"mode": "palette-classic"},
             "mappings": [],
             "thresholds": {
               "mode": "absolute",
               "steps": [{"color": "green", "value": null}]
             }
           }
         },
         "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
         "id": 1,
         "options": {
           "legend": {"calcs": [], "displayMode": "list", "placement": "bottom"},
           "tooltip": {"mode": "single"}
         },
         "targets": [
           {
             "expr": "rate(http_requests_total[5m])",
             "legendFormat": "{{method}} {{endpoint}}"
           }
         ],
         "title": "Request Rate",
         "type": "timeseries"
       },
       {
         "datasource": "Prometheus",
         "fieldConfig": {
           "defaults": {
             "color": {"mode": "thresholds"},
             "mappings": [],
             "thresholds": {
               "mode": "absolute",
               "steps": [
                 {"color": "green", "value": null},
                 {"color": "yellow", "value": 0.5},
                 {"color": "red", "value": 1}
               ]
             },
             "unit": "s"
           }
         },
         "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
         "id": 2,
         "options": {
           "orientation": "auto",
           "reduceOptions": {"calcs": ["lastNotNull"], "fields": "", "values": false},
           "showThresholdLabels": false,
           "showThresholdMarkers": true
         },
         "targets": [
           {
             "expr": "histogram_quantile(0.95, http_request_duration_seconds_bucket)"
           }
         ],
         "title": "P95 Latency",
         "type": "gauge"
       },
       {
         "datasource": "Prometheus",
         "fieldConfig": {
           "defaults": {
             "color": {"mode": "palette-classic"},
             "mappings": []
           }
         },
         "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8},
         "id": 3,
         "options": {
           "legend": {"displayMode": "table", "placement": "right"},
           "pieType": "pie",
           "tooltip": {"mode": "single"}
         },
         "targets": [
           {
             "expr": "sum by (model) (api_requests_total)"
           }
         ],
         "title": "Requests by Model",
         "type": "piechart"
       }
     ],
     "schemaVersion": 27,
     "style": "dark",
     "tags": ["api-gateway"],
     "templating": {"list": []},
     "time": {"from": "now-1h", "to": "now"},
     "timepicker": {},
     "timezone": "",
     "title": "API Gateway Dashboard",
     "uid": "api-gateway",
     "version": 0
   }
   ```

5. 创建系统指标仪表盘（deployments/monitoring/grafana/dashboards/system-metrics.json）：
   ```json
   {
     "panels": [
       {
         "datasource": "Prometheus",
         "fieldConfig": {
           "defaults": {
             "color": {"mode": "palette-classic"},
             "unit": "short"
           }
         },
         "gridPos": {"h": 8, "w": 8, "x": 0, "y": 0},
         "id": 1,
         "targets": [
           {"expr": "go_goroutines"}
         ],
         "title": "Goroutines",
         "type": "timeseries"
       },
       {
         "datasource": "Prometheus",
         "fieldConfig": {
           "defaults": {
             "color": {"mode": "palette-classic"},
             "unit": "bytes"
           }
         },
         "gridPos": {"h": 8, "w": 8, "x": 8, "y": 0},
         "id": 2,
         "targets": [
           {"expr": "go_memstats_alloc_bytes"}
         ],
         "title": "Memory Allocation",
         "type": "timeseries"
       },
       {
         "datasource": "Prometheus",
         "fieldConfig": {
           "defaults": {
             "color": {"mode": "palette-classic"},
             "unit": "short"
           }
         },
         "gridPos": {"h": 8, "w": 8, "x": 16, "y": 0},
         "id": 3,
         "targets": [
           {"expr": "concurrent_connections"}
         ],
         "title": "Concurrent Connections",
         "type": "timeseries"
       }
     ],
     "title": "System Metrics",
     "uid": "system-metrics",
     "version": 0
   }
   ```
  </action>
  <verify>
    <automated>docker exec ai-gateway-grafana curl -s http://localhost:3000/api/health | grep -q "ok"</automated>
  </verify>
  <done>Grafana 仪表盘配置完成，成功连接 Prometheus 和 Loki，正确显示指标</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Loki 日志聚合</name>
  <files>deployments/monitoring/loki.yml, deployments/monitoring/promtail.yml, internal/logging/logger.go</files>
  <behavior>
    - 测试 1: Loki 成功接收日志
    - 测试 2: Promtail 成功发送日志到 Loki
    - 测试 3: Grafana 可以查询 Loki 日志
  </behavior>
  <action>
创建 Loki 日志聚合配置和结构化日志：

1. 创建 Loki 配置（deployments/monitoring/loki.yml）：
   ```yaml
   auth_enabled: false

   server:
     http_listen_port: 3100

   ingester:
     lifecycler:
       address: 127.0.0.1
       ring:
         kvstore:
           store: inmemory
       replication_factor: 1
     chunk_idle_period: 1h
     max_chunk_age: 1h

   schema_config:
     configs:
       - from: 2020-10-24
         store: boltdb-shipper
         object_store: filesystem
         schema: v11
         index:
           prefix: index_
           period: 24h

   storage_config:
     boltdb_shipper:
       active_index_directory: /tmp/loki/boltdb-shipper-active
       cache_location: /tmp/loki/boltdb-shipper-cache
       shared_directory: /tmp/loki/boltdb-shipper-shared
     filesystem:
       directory: /tmp/loki/chunks

   limits_config:
     enforce_metric_name: false
     reject_old_samples: true
     reject_old_samples_max_age: 168h

   chunk_store_config:
     max_look_back_period: 0s

   table_manager:
     retention_deletes_enabled: false
     retention_period: 0s
   ```

2. 创建 Promtail 配置（deployments/monitoring/promtail.yml）：
   ```yaml
   server:
     http_listen_port: 9080

   positions:
     filename: /tmp/positions.yaml

   clients:
     - url: http://loki:3100/loki/api/v1/push

   scrape_configs:
     - job_name: ai-gateway-logs
       static_configs:
         - targets:
             - localhost
           labels:
             job: ai-gateway
             environment: development
       pipeline_stages:
         - json:
             expressions:
               level: level
               msg: message
               time: timestamp
               request_id: request_id
               user_id: user_id
         - labels:
             level:
             request_id:
             user_id:
   ```

3. 创建结构化日志（internal/logging/logger.go）：
   ```go
   package logging

   import (
       "context"
       "os"
       "time"

       "github.com/google/uuid"
       "go.uber.org/zap"
       "go.uber.org/zap/zapcore"
   )

   var (
       logger *zap.Logger
       sugar  *zap.SugaredLogger
   )

   // InitLogger 初始化日志
   func InitLogger(env string) error {
       var config zap.Config

       if env == "production" {
           config = zap.NewProductionConfig()
       } else {
           config = zap.NewDevelopmentConfig()
       }

       config.EncoderConfig.TimeKey = "timestamp"
       config.EncoderConfig.EncodeTime = zapcore.ISO8601TimeEncoder

       var err error
       logger, err = config.Build()
       if err != nil {
           return err
       }

       sugar = logger.Sugar()
       return nil
   }

   // WithRequestID 添加请求 ID 到 context
   func WithRequestID(ctx context.Context) context.Context {
       reqID := ctx.Value("request_id")
       if reqID == nil {
           reqID = uuid.New().String()
       }
       return context.WithValue(ctx, "request_id", reqID)
   }

   // GetRequestID 从 context 获取请求 ID
   func GetRequestID(ctx context.Context) string {
       if reqID, ok := ctx.Value("request_id").(string); ok {
           return reqID
       }
       return ""
   }

   // LogWithFields 记录带字段的日志
   func LogWithFields(ctx context.Context, msg string, fields ...zap.Field) {
       allFields := append([]zap.Field{
           zap.String("request_id", GetRequestID(ctx)),
           zap.Time("timestamp", time.Now()),
       }, fields...)
       logger.Info(msg, allFields...)
   }

   // Info 记录信息日志
   func Info(msg string, fields ...zap.Field) {
       logger.Info(msg, fields...)
   }

   // Error 记录错误日志
   func Error(msg string, fields ...zap.Field) {
       logger.Error(msg, fields...)
   }

   // Warn 记录警告日志
   func Warn(msg string, fields ...zap.Field) {
       logger.Warn(msg, fields...)
   }

   // Debug 记录调试日志
   func Debug(msg string, fields ...zap.Field) {
       logger.Debug(msg, fields...)
   }

   // Sync 刷新日志缓冲
   func Sync() error {
       return logger.Sync()
   }
   ```

4. 更新日志中间件输出 JSON 格式：
   ```go
   // internal/middleware/logger.go
   func Logger() fiber.Handler {
       return func(c fiber.Ctx) error {
           start := time.Now()

           // 处理请求
           err := c.Next()

           // 记录日志
           duration := time.Since(start)

           logging.Info("HTTP Request",
               zap.String("method", c.Method()),
               zap.String("path", c.Path()),
               zap.Int("status", c.Response().StatusCode()),
               zap.Duration("duration", duration),
               zap.String("request_id", c.Locals("request_id").(string)),
               zap.String("user_id", c.Locals("user_id").(string)),
           )

           return err
       }
   }
   ```

5. 更新 main.go 初始化日志：
   ```go
   if err := logging.InitLogger(os.Getenv("ENV")); err != nil {
       log.Fatal("Failed to initialize logger:", err)
   }
   defer logging.Sync()
   ```
  </action>
  <verify>
    <automated>docker logs ai-gateway-loki 2>&1 | grep -q "Loki started"</automated>
  </verify>
  <done>Loki 日志聚合配置完成，结构化日志正确发送和存储</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: 告警系统实现</name>
  <files>internal/alerting/alerts.go, internal/alerting/notifiers.go</files>
  <behavior>
    - 测试 1: 告警规则正确加载
    - 测试 2: 告警触发时发送通知
    - 测试 3: 告警恢复时发送恢复通知
  </behavior>
  <action>
实现告警系统，支持多种通知方式：

1. 创建告警类型定义（internal/alerting/alerts.go）：
   ```go
   package alerting

   import (
       "context"
       "time"

       "github.com/go-co-op/gocron/v2"
   )

   type AlertLevel string

   const (
       AlertLevelInfo     AlertLevel = "info"
       AlertLevelWarning  AlertLevel = "warning"
       AlertLevelCritical AlertLevel = "critical"
   )

   type Alert struct {
       ID          string            `json:"id"`
       Level       AlertLevel        `json:"level"`
       Title       string            `json:"title"`
       Description string            `json:"description"`
       Labels      map[string]string `json:"labels"`
       Timestamp   time.Time         `json:"timestamp"`
       Resolved    bool              `json:"resolved"`
   }

   type Notifier interface {
       Send(ctx context.Context, alert Alert) error
   }

   type AlertManager struct {
       notifiers []Notifier
       scheduler gocron.Scheduler
   }

   func NewAlertManager(notifiers ...Notifier) *AlertManager {
       return &AlertManager{
           notifiers: notifiers,
       }
   }

   // SendAlert 发送告警
   func (am *AlertManager) SendAlert(ctx context.Context, alert Alert) error {
       for _, notifier := range am.notifiers {
           if err := notifier.Send(ctx, alert); err != nil {
               // 记录发送失败，但不影响其他通知
               continue
           }
       }
       return nil
   }

   // CheckAndAlert 检查条件并发送告警
   func (am *AlertManager) CheckAndAlert(
       ctx context.Context,
       condition func() (bool, error),
       alert Alert,
   ) {
       triggered, err := condition()
       if err != nil {
           return
       }

       if triggered {
           alert.Timestamp = time.Now()
           am.SendAlert(ctx, alert)
       }
   }
   ```

2. 创建通知器实现（internal/alerting/notifiers.go）：
   ```go
   package alerting

   import (
       "bytes"
       "context"
       "encoding/json"
       "fmt"
       "net/http"
       "net/smtp"
   )

   // EmailNotifier 邮件通知器
   type EmailNotifier struct {
       SMTPHost     string
       SMTPPort     int
       Username     string
       Password     string
       From         string
       To           []string
   }

   func (n *EmailNotifier) Send(ctx context.Context, alert Alert) error {
       subject := fmt.Sprintf("[%s] %s", alert.Level, alert.Title)
       body := fmt.Sprintf("Description: %s\nLabels: %v", alert.Description, alert.Labels)

       msg := fmt.Sprintf("From: %s\r\nTo: %s\r\nSubject: %s\r\n\r\n%s",
           n.From,
           fmt.Sprintf("%v", n.To),
           subject,
           body,
       )

       auth := smtp.PlainAuth("", n.Username, n.Password, n.SMTPHost)
       return smtp.SendMail(
           fmt.Sprintf("%s:%d", n.SMTPHost, n.SMTPPort),
           auth,
           n.From,
           n.To,
           []byte(msg),
       )
   }

   // WebhookNotifier Webhook 通知器
   type WebhookNotifier struct {
       URL     string
       Headers map[string]string
   }

   func (n *WebhookNotifier) Send(ctx context.Context, alert Alert) error {
       data, err := json.Marshal(alert)
       if err != nil {
           return err
       }

       req, err := http.NewRequestWithContext(ctx, "POST", n.URL, bytes.NewReader(data))
       if err != nil {
           return err
       }

       req.Header.Set("Content-Type", "application/json")
       for k, v := range n.Headers {
           req.Header.Set(k, v)
       }

       client := &http.Client{Timeout: 10 * time.Second}
       resp, err := client.Do(req)
       if err != nil {
           return err
       }
       defer resp.Body.Close()

       if resp.StatusCode >= 400 {
           return fmt.Errorf("webhook returned status %d", resp.StatusCode)
       }

       return nil
   }

   // SlackNotifier Slack 通知器
   type SlackNotifier struct {
       WebhookURL string
       Channel    string
   }

   func (n *SlackNotifier) Send(ctx context.Context, alert Alert) error {
       color := map[AlertLevel]string{
           AlertLevelInfo:     "good",
           AlertLevelWarning:  "warning",
           AlertLevelCritical: "danger",
       }[alert.Level]

       payload := map[string]interface{}{
           "channel": n.Channel,
           "attachments": []map[string]interface{}{
               {
                   "color":  color,
                   "title":  alert.Title,
                   "text":   alert.Description,
                   "fields": []map[string]string{
                       {title: "Level", value: string(alert.Level)},
                       {title: "Time", value: alert.Timestamp.Format(time.RFC3339)},
                   },
               },
           },
       }

       data, err := json.Marshal(payload)
       if err != nil {
           return err
       }

       req, err := http.NewRequestWithContext(ctx, "POST", n.WebhookURL, bytes.NewReader(data))
       if err != nil {
           return err
       }

       req.Header.Set("Content-Type", "application/json")

       client := &http.Client{Timeout: 10 * time.Second}
       resp, err := client.Do(req)
       if err != nil {
           return err
       }
       defer resp.Body.Close()

       return nil
   }
   ```

3. 在 main.go 中初始化告警管理器：
   ```go
   import (
       "github.com/yockii/ai-gateway/internal/alerting"
   )

   func main() {
       // ... 现有代码

       // 创建通知器
       emailNotifier := &alerting.EmailNotifier{
           SMTPHost: os.Getenv("SMTP_HOST"),
           SMTPPort: 587,
           Username: os.Getenv("SMTP_USERNAME"),
           Password: os.Getenv("SMTP_PASSWORD"),
           From:     "alerts@ai-gateway.com",
           To:       []string{os.Getenv("ALERT_EMAIL")},
       }

       slackNotifier := &alerting.SlackNotifier{
           WebhookURL: os.Getenv("SLACK_WEBHOOK_URL"),
           Channel:    "#alerts",
       }

       // 创建告警管理器
       alertManager := alerting.NewAlertManager(emailNotifier, slackNotifier)

       // 定期检查告警条件
       go func() {
           ticker := time.NewTicker(1 * time.Minute)
           for range ticker.C {
               // 检查错误率
               alertManager.CheckAndAlert(context.Background(),
                   func() (bool, error) {
                       // 查询 Prometheus 获取错误率
                       // 这里简化实现
                       return false, nil
                   },
                   alerting.Alert{
                       Level:       alerting.AlertLevelWarning,
                       Title:       "High Error Rate",
                       Description: "Error rate exceeds 5%",
                   },
               )
           }
       }()
   }
   ```
  </action>
  <verify>
    <automated>cd /d/projects/github.com/yockii/ai_gateway && go build ./internal/alerting/...</automated>
  </verify>
  <done>告警系统实现完成，支持邮件、Webhook 和 Slack 通知方式</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: 运维大屏数据 API</name>
  <files>pkg/handlers/monitoring.go, pkg/router/router.go</files>
  <behavior>
    - 测试 1: /admin/monitoring/metrics 返回系统指标
    - 测试 2: /admin/monitoring/alerts 返回告警列表
    - 测试 3: 数据每 30 秒更新一次
  </behavior>
  <action>
创建运维大屏数据 API：

```go
package handlers

import (
    "context"
    "time"

    "github.com/gofiber/fiber/v3"
    "github.com/prometheus/client_golang/api"
    v1 "github.com/prometheus/client_golang/api/prometheus/v1"
    "github.com/prometheus/common/model"
)

type MonitoringHandler struct {
    prometheusAPI v1.API
}

func NewMonitoringHandler(prometheusURL string) *MonitoringHandler {
    client, err := api.NewClient(api.Config{
        Address: prometheusURL,
    })
    if err != nil {
        panic(err)
    }

    return &MonitoringHandler{
        prometheusAPI: v1.NewAPI(client),
    }
}

// GetSystemMetrics 获取系统指标
func (h *MonitoringHandler) GetSystemMetrics(c fiber.Ctx) error {
    ctx, cancel := context.WithTimeout(c.Context(), 10*time.Second)
    defer cancel()

    // 查询 QPS
    qps, _, err := h.prometheusAPI.Query(ctx, "rate(http_requests_total[5m])", time.Now())
    if err != nil {
        return c.Status(500).JSON(fiber.Map{"error": err.Error()})
    }

    // 查询平均延迟
    latency, _, err := h.prometheusAPI.Query(ctx, "histogram_quantile(0.95, http_request_duration_seconds_bucket)", time.Now())
    if err != nil {
        return c.Status(500).JSON(fiber.Map{"error": err.Error()})
    }

    // 查询错误率
    errorRate, _, err := h.prometheusAPI.Query(ctx, "rate(http_requests_total{status=~\"5..\"}[5m]) / rate(http_requests_total[5m])", time.Now())
    if err != nil {
        return c.Status(500).JSON(fiber.Map{"error": err.Error()})
    }

    // 查询活跃用户数
    activeUsers, _, err := h.prometheusAPI.Query(ctx, "active_users", time.Now())
    if err != nil {
        return c.Status(500).JSON(fiber.Map{"error": err.Error()})
    }

    return c.JSON(fiber.Map{
        "qps":         extractSampleValue(qps),
        "avg_latency": extractSampleValue(latency),
        "error_rate":  extractSampleValue(errorRate),
        "active_users": extractSampleValue(activeUsers),
        "timestamp":   time.Now(),
    })
}

// GetAlerts 获取告警列表
func (h *MonitoringHandler) GetAlerts(c fiber.Ctx) error {
    // 从 Prometheus 查询告警
    // 这里简化实现，实际应从 Alertmanager 查询
    return c.JSON([]fiber.Map{
        {
            "level":       "warning",
            "title":       "High Latency",
            "description": "P95 latency exceeds 1s",
            "timestamp":   time.Now().Add(-5 * time.Minute),
        },
    })
}

// GetLogs 获取日志列表
func (h *MonitoringHandler) GetLogs(c fiber.Ctx) error {
    // 从 Loki 查询日志
    // 这里简化实现
    return c.JSON([]fiber.Map{
        {
            "timestamp": time.Now().Add(-1 * time.Minute),
            "level":     "info",
            "message":   "Request processed successfully",
            "request_id": "req-123",
        },
    })
}

func extractSampleValue(result interface{}) float64 {
    if vector, ok := result.(model.Vector); ok && len(vector) > 0 {
        return float64(vector[0].Value)
    }
    return 0
}
```

在 router.go 中注册路由：
```go
monitoringHandler := handlers.NewMonitoringHandler("http://prometheus:9090")

admin.Get("/monitoring/metrics", monitoringHandler.GetSystemMetrics)
admin.Get("/monitoring/alerts", monitoringHandler.GetAlerts)
admin.Get("/monitoring/logs", monitoringHandler.GetLogs)
```
  </action>
  <verify>
    <automated>cd /d/projects/github.com/yockii/ai_gateway && go build ./pkg/handlers/...</automated>
  </verify>
  <done>运维大屏数据 API 实现完成，前端可以通过 API 获取监控数据</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API Gateway → Prometheus | Metrics cross here via HTTP |
| Promtail → Loki | Logs cross here via HTTP |
| AlertManager → Notifiers | Alerts cross here via SMTP/Webhook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | Spoofing | Prometheus | mitigate | Prometheus 部署在内网，不对外暴露；/metrics 端点不包含敏感信息 |
| T-03-24 | Tampering | Metrics | mitigate | Prometheus 指标只读，不接受外部输入 |
| T-03-25 | Information Disclosure | Logs | mitigate | 日志不包含敏感信息（API Key、密码）；访问 Loki 需要认证 |
| T-03-26 | Denial of Service | Log volume | mitigate | Loki 配置日志保留期限和大小限制 |
| T-03-27 | Tampering | Alert notifications | mitigate | Webhook 使用 HTTPS 验证；邮件使用 SMTP TLS |
| T-03-28 | Information Disclosure | Grafana | mitigate | Grafana 配置认证和访问控制；匿名访问只读 |
</threat_model>

<verification>
1. Prometheus 成功抓取 /metrics 端点
2. Prometheus UI 可以查询指标
3. Grafana 成功连接 Prometheus
4. Grafana 仪表盘正确显示指标
5. Loki 成功接收和存储日志
6. Promtail 成功发送日志
7. Grafana 成功连接 Loki
8. Grafana 可以查询日志
9. 告警规则正确加载
10. 运维大屏 API 正确返回数据
</verification>

<success_criteria>
1. Prometheus 成功采集 Go 应用指标
2. Prometheus UI 可以查询所有指标
3. Grafana 仪表盘正确显示 API 和系统指标
4. Loki 成功收集应用日志
5. Grafana 可以查询和过滤日志
6. 告警规则配置正确
7. 告警触发时发送通知
8. 监控系统数据持久化
9. Docker Compose 一键启动所有监控组件
10. 运维大屏 API 返回实时数据
</success_criteria>

<output>
After completion, create `.planning/phases/03-高级功能开发/03-04-SUMMARY.md`
</output>
